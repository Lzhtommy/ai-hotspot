---
phase: quick-260425-kg7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/feed/feed-tabs.tsx
  - src/components/feed/feed-top-bar.tsx
  - src/app/(reader)/page.tsx
  - src/app/(reader)/all/page.tsx
  - src/app/(reader)/favorites/page.tsx
  - tests/unit/feed-tabs.test.tsx
autonomous: true
requirements:
  - QUICK-260425-kg7
must_haves:
  truths:
    - 未登录用户点击顶部「收藏」tab 不再跳转到 /favorites,而是触发现有的 LoginPromptModal(同卡片星标按钮的体验)。
    - 已登录用户点击「收藏」tab 行为不变 — 仍然 next/link 导航到 /favorites。
    - 未登录用户直接访问 /favorites URL 仍被 server `redirect('/')` 兜底(深度链接保护)。
    - FeedTabs 视觉、键盘 focus、aria-current 在登录与未登录两种渲染分支下保持一致。
    - LoginPromptModal 不需要任何改动 — 已在 (reader)/layout.tsx 监听 `open-login-modal`。
  artifacts:
    - path: src/components/feed/feed-tabs.tsx
      provides: "Client Component('use client') with optional `isAuthenticated` prop; renders 收藏 tab as <button> when false"
      contains: "'use client'"
    - path: src/components/feed/feed-top-bar.tsx
      provides: "Pass-through `isAuthenticated?: boolean` prop forwarded to <FeedTabs/>"
    - path: src/app/(reader)/page.tsx
      provides: "Derives isAuthenticated from session and passes to FeedTopBar"
    - path: src/app/(reader)/all/page.tsx
      provides: "Derives isAuthenticated from session and passes to FeedTopBar"
    - path: src/app/(reader)/favorites/page.tsx
      provides: "Derives isAuthenticated from session and passes to FeedTopBar (anonymous redirect retained)"
    - path: tests/unit/feed-tabs.test.tsx
      provides: "Coverage for both anonymous (button) and authenticated (Link) branches"
  key_links:
    - from: src/components/feed/feed-tabs.tsx
      to: document event 'open-login-modal'
      via: "document.dispatchEvent(new CustomEvent('open-login-modal')) in onClick"
      pattern: "open-login-modal"
    - from: src/app/(reader)/page.tsx
      to: src/components/feed/feed-top-bar.tsx
      via: "<FeedTopBar isAuthenticated={isAuthenticated} ... />"
      pattern: "isAuthenticated"
    - from: src/components/feed/feed-top-bar.tsx
      to: src/components/feed/feed-tabs.tsx
      via: "<FeedTabs isAuthenticated={isAuthenticated} ... />"
      pattern: "FeedTabs"
---

<objective>
修复未登录用户点击顶部「收藏」tab 时被 server `redirect('/')` 弹回精选页的体验断点。改为复用与卡片星标按钮一致的 anonymous 模式 — 在 client 端 dispatch `open-login-modal` 事件,由已挂载在 (reader)/layout.tsx 的 LoginPromptModal 处理。

Purpose: 顶部 tab 与卡片操作的 anonymous 行为统一,避免无意义的页面跳转;不暴露未保护的 /favorites 视觉(redirect 仍保留作深链兜底)。
Output: FeedTabs 提升为 Client Component 并新增可选 `isAuthenticated` prop;FeedTopBar 透传;三个 reader feed 页面派生并下传 `isAuthenticated`;新增测试覆盖两个渲染分支。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/components/feed/feed-tabs.tsx
@src/components/feed/feed-top-bar.tsx
@src/components/feed/feed-card-actions.tsx
@src/components/feed/login-prompt-modal.tsx
@src/app/(reader)/page.tsx
@src/app/(reader)/all/page.tsx
@src/app/(reader)/favorites/page.tsx

<interfaces>
<!-- Existing anonymous-modal seam (Phase 4 D-26 / Phase 5 Plan 05-07) -->
<!-- LoginPromptModal listens on `document` for the CustomEvent 'open-login-modal'. -->
<!-- Mounted once in src/app/(reader)/layout.tsx — every reader page inherits it. -->

From src/components/feed/feed-card-actions.tsx (canonical pattern to mirror):
```ts
function openLoginModal() {
  // Phase 4 D-26 seam — LoginPromptModal listens for this on `document`.
  document.dispatchEvent(new CustomEvent('open-login-modal'));
}
```

From src/components/feed/feed-tabs.tsx (current props — extend, don't replace):
```ts
interface FeedTabsProps {
  pathname: string;
  counts?: { featured?: number; all?: number; favorites?: number };
}
```

From src/components/feed/feed-top-bar.tsx (current FeedTopBarProps — add isAuthenticated):
```ts
export interface FeedTopBarProps {
  view: 'featured' | 'all' | 'favorites';
  count?: number;
  totalCount?: number;
  lastSyncMinutes?: number | null;
  pathname: string;
  counts?: { featured?: number; all?: number; favorites?: number };
  subtitle?: string;
  canSync?: boolean;
}
```

From all three reader pages (existing session derivation pattern):
```ts
const session = await auth();
const isAuthenticated = !!session?.user?.id;
// canSync already extracted same way — mirror exactly.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeedTabs 转 Client Component + isAuthenticated prop + anonymous 收藏 tab 渲染为 button</name>
  <files>src/components/feed/feed-tabs.tsx, tests/unit/feed-tabs.test.tsx</files>
  <behavior>
    - 测试 1: `isAuthenticated=false` 时,收藏 tab 渲染为 `<button type="button">`,**没有 `href` 属性**,DOM tag 是 BUTTON。
    - 测试 2: `isAuthenticated=false` 时,点击收藏 tab 触发 `document` 上的 `open-login-modal` CustomEvent(用 `addEventListener` spy 验证)。
    - 测试 3: `isAuthenticated=true` 时,收藏 tab 仍是 `<a>` (Link) 且 `href="/favorites"`。
    - 测试 4: `isAuthenticated` 未传时(undefined,默认 false 行为),收藏 tab 渲染为 button — 这与 anonymous 默认安全语义一致。
    - 测试 5: 三个 tab 的 active 状态、aria-current、视觉样式(borderBottom / color)在 button 与 Link 两种渲染分支下都保持一致 —— 至少断言 active tab 的 `aria-current="page"` 在 button 分支也存在。
    - 测试 6: 精选(/) 与 全部动态(/all) tab 在两种 isAuthenticated 分支下都仍是 `<a>` Link(只有收藏受影响)。
  </behavior>
  <action>
    1. 在 `src/components/feed/feed-tabs.tsx` 顶部加 `'use client'` 指令(组件提升为 Client Component;Next.js App Router 允许 RSC 直接 import client components,FeedTopBar 调用方无需修改)。
    2. 扩展 `FeedTabsProps`:新增 `isAuthenticated?: boolean`(可选,默认 false — 默认安全:未传等同未登录,与 RSC 调用方未及时传 prop 时的兜底语义一致)。
    3. tabs 数组保持不变(label/href/active/count)。
    4. 渲染 map 中,对收藏 tab(href === '/favorites')做条件分支:
       - 当 `isAuthenticated === true` → 仍渲染 `<Link href="/favorites">`,行为完全不变(保持现有 style/aria-current/count 渲染)。
       - 当 `isAuthenticated !== true`(false / undefined) → 渲染 `<button type="button" onClick={openLoginModal}>`,**复用与 Link 完全一致的 inline style**(把 style 对象提到 map callback 顶部 const 或抽出变量,避免两个分支 style 漂移)、`aria-current` 行为(active 时仍为 'page')、相同的 children(`{t.count != null ? \`${t.label} (${t.count})\` : t.label}`)。**注意 button 不能有 href;不能用 `<Link>`**。键盘焦点视觉与其它 tab 一致(浏览器原生 focus-visible 在 button 与 a 上均生效;不要加 outline:none)。
    5. 新增内部 helper `function openLoginModal() { document.dispatchEvent(new CustomEvent('open-login-modal')); }` —— 与 `feed-card-actions.tsx` 同名同语义(D-26 seam)。
    6. 精选 / 全部动态 tab 不动,继续 `<Link>`。
    7. 创建 `tests/unit/feed-tabs.test.tsx`:
       - 文件头注释引用 quick 任务 ID(`260425-kg7`)与 Phase 4/5 D-26 seam。
       - 用 `@testing-library/react` 的 `render` + `screen.getByRole`。
       - 用 `vi.fn()` + `document.addEventListener('open-login-modal', spy)` 验证 click 派发事件;**记得在 afterEach removeEventListener**,避免跨测试串扰。
       - 断言 anonymous 分支下查询收藏 tab 用 `screen.getByRole('button', { name: /收藏/ })`,authenticated 分支用 `screen.getByRole('link', { name: /收藏/ })` 并断言 `getAttribute('href') === '/favorites'`。
       - 断言精选 tab 在两种分支都是 `getByRole('link', { name: /精选/ })` —— 防止误改其它 tab。
    8. **不引入新依赖**;不改 LoginPromptModal;不改 sidebar 导航;不改卡片星标。
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/feed-tabs.test.tsx</automated>
  </verify>
  <done>
    - feed-tabs.tsx 顶部有 'use client';新增 isAuthenticated?: boolean prop。
    - 收藏 tab 在 isAuthenticated=false / undefined 时渲染为 button(无 href)且点击 dispatch open-login-modal 事件。
    - 收藏 tab 在 isAuthenticated=true 时仍是 Link → /favorites。
    - 精选 / 全部动态 tab 在两种分支下都是 Link,行为不变。
    - tests/unit/feed-tabs.test.tsx 6 条断言全部 pass(button vs link、href 缺失、事件派发、aria-current、其它两 tab 不受影响)。
    - 整组 vitest 不出现新的失败(运行整套 `pnpm test` Task 3 校验)。
  </done>
</task>

<task type="auto">
  <name>Task 2: FeedTopBar 透传 isAuthenticated + 三个 reader 页面派生并下传</name>
  <files>src/components/feed/feed-top-bar.tsx, src/app/(reader)/page.tsx, src/app/(reader)/all/page.tsx, src/app/(reader)/favorites/page.tsx</files>
  <action>
    1. `src/components/feed/feed-top-bar.tsx`:
       - `FeedTopBarProps` 新增可选字段 `isAuthenticated?: boolean`(放在 `canSync` 旁边,加 JSDoc 说明:"Phase 5 quick 260425-kg7: 用于让 FeedTabs 在未登录时把「收藏」tab 渲染为 button 并 dispatch open-login-modal,而非跳转 /favorites 被 redirect 弹回。默认 false。")。
       - 函数签名解构默认值 `isAuthenticated = false`。
       - 末尾 `<FeedTabs pathname={pathname} counts={counts} />` 改为 `<FeedTabs pathname={pathname} counts={counts} isAuthenticated={isAuthenticated} />`。
       - 不动其它逻辑(subtitle override、canSync、syncLabel、布局样式都不变)。
    2. `src/app/(reader)/page.tsx`(精选):
       - `isAuthenticated` 已在文件中存在(第 28 行)— 直接把它作为 prop 加到现有 `<FeedTopBar ... canSync={canSync} />`,变成 `<FeedTopBar ... canSync={canSync} isAuthenticated={isAuthenticated} />`。
    3. `src/app/(reader)/all/page.tsx`:
       - 同上:`isAuthenticated` 已存在(第 45 行)— 在 `<FeedTopBar>` 上追加 `isAuthenticated={isAuthenticated}` prop。
    4. `src/app/(reader)/favorites/page.tsx`:
       - 此页面 redirect anonymous 用户到 / —— 进入到渲染 FeedTopBar 时一定是已登录的。直接传字面量 `isAuthenticated={true}` 到 FeedTopBar(显式优于隐式;不依赖默认 false 的兜底)。**保留** redirect 兜底逻辑不动 — 防止有人深链 `/favorites`。
    5. 不改 LoginPromptModal、不改 (reader)/layout.tsx、不改 sidebar、不改卡片星标。不引入新 import。
    6. 验证编译 + 类型:`pnpm tsc --noEmit`(或项目 typecheck 脚本)。验证整套测试无回归。
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test</automated>
  </verify>
  <done>
    - FeedTopBarProps 新增 isAuthenticated?: boolean(默认 false),透传给 FeedTabs。
    - / 与 /all 页面派生 isAuthenticated 并下传(已存在的变量复用)。
    - /favorites 页面下传 isAuthenticated={true}(redirect 兜底保留)。
    - tsc 通过、整套 vitest 通过(包括 Task 1 新增的 feed-tabs 测试 + 现有 FeedTopBar/FeedCard/LoginPromptModal 测试无回归)。
    - 手动浏览验证:未登录访问 / 或 /all,点击顶部「收藏」tab → 弹出登录 modal(不跳转,URL 不变);登录后点击 → 正常跳到 /favorites。直接访问 /favorites(未登录)→ 仍被 redirect 到 /(深链兜底未破坏)。
  </done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/unit/feed-tabs.test.tsx` 全绿(Task 1 新测试)。
- `pnpm tsc --noEmit` 无新错误。
- `pnpm test` 整套无回归(尤其 LoginPromptModal、FeedCardActions、UserChip 相关测试不受影响)。
- Anonymous 行为统一:卡片星标 click → modal;tab 收藏 click → modal(同事件、同 modal、同 dismiss 路径)。
- /favorites 深链兜底未破坏:未登录直接访问该 URL 仍被 server `redirect('/')`。
- FeedTabs 视觉/键盘焦点在 button 与 Link 两种分支下一致(active borderBottom + ink-900 color + aria-current="page")。
</verification>

<success_criteria>
- 未登录用户在 / 或 /all 点击顶部「收藏」tab → 不发生导航,LoginPromptModal 弹出。
- 已登录用户在 / 或 /all 点击「收藏」tab → next/link 客户端导航到 /favorites,行为完全保持原样。
- 未登录用户直接打开 /favorites URL → server 端 `redirect('/')` 把他送回精选(已存在,未改动)。
- 「精选」「全部动态」两 tab 行为完全不变。
- 不引入新依赖、不改 LoginPromptModal、不改 sidebar 导航、不改卡片星标 onClick。
- 所有 vitest + tsc 通过。
</success_criteria>

<output>
After completion, create `.planning/quick/260425-kg7-favorites-tab-anon-login-modal/260425-kg7-SUMMARY.md` summarizing:
- Files changed (5 source + 1 new test)
- Anonymous-tab behavior unification rationale (mirror cards' D-26 seam)
- /favorites server redirect retained as deep-link fallback
- Test coverage notes (button vs Link branches; event-dispatch spy)
</output>
