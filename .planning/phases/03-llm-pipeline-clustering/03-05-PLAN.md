---
phase: 03-llm-pipeline-clustering
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - scripts/verify-llm.ts
  - package.json
  - .planning/phases/03-llm-pipeline-clustering/03-UAT.md
autonomous: false
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06
  - LLM-07
  - LLM-08
  - LLM-09
  - LLM-10
  - LLM-11
  - LLM-12
  - LLM-13
  - CLUST-01
  - CLUST-02
  - CLUST-03
  - CLUST-04
  - CLUST-05
  - CLUST-06
  - CLUST-07
tags: [verification, uat, harness, live-smoke]

must_haves:
  truths:
    - "SC#1: A pending English-source item → after runProcessItem → status='published' with title_zh non-empty AND summary_zh non-empty AND score∈[0,100] AND recommendation non-empty AND tags.length >= 1"
    - "SC#2: SELECT MAX(cache_read_tokens) FROM pipeline_runs WHERE item_id IN (<sentinel-A-id>, <sentinel-B-id>) > 0 (caching active on the second item within 5 min of the first)"
    - "SC#3: A seeded malformed-fixture item → status='dead_letter' AND status <> 'published' AND items.failure_reason is non-null"
    - "SC#4: Two sentinel items seeded with similar embeddings (cosine > threshold) cross different source_ids → items.cluster_id is equal AND clusters.member_count >= 2 (after refresh-clusters runs) AND clusters.primary_item_id = items.id WHERE published_at = MIN"
    - "SC#5: UAT gate — human opens Langfuse dashboard, sees a trace per sentinel item with non-zero estimated cost + Anthropic + Voyage spans (manual confirmation)"
    - "Cleanup: all sentinel sources + items + pipeline_runs + clusters created by verify:llm are removed in finally{} regardless of assertion outcome (mirror verify-ingest.ts:386-390)"
  artifacts:
    - path: "scripts/verify-llm.ts"
      provides: "Live SC harness — seeds sentinels, runs pipeline via runProcessItem, asserts SC#1-4, prints Langfuse URL for SC#5"
      contains: "verifyLLM"
    - path: "package.json"
      provides: "verify:llm script"
      contains: "verify:llm"
    - path: ".planning/phases/03-llm-pipeline-clustering/03-UAT.md"
      provides: "Human UAT checklist — 5 ROADMAP SCs with exact verification steps"
      contains: "SC#5"
  key_links:
    - from: "scripts/verify-llm.ts"
      to: "src/lib/llm/process-item-core.ts"
      via: "import runProcessItem"
      pattern: "runProcessItem"
    - from: "scripts/verify-llm.ts"
      to: "src/lib/cluster/refresh.ts"
      via: "import runRefreshClusters (run inline after pipeline to assert SC#4 member_count)"
      pattern: "runRefreshClusters"
    - from: "scripts/verify-llm.ts"
      to: "pipeline_runs table"
      via: "SELECT MAX(cache_read_tokens) query for SC#2"
      pattern: "cache_read_tokens"
---

<objective>
Ship the live verification harness `scripts/verify-llm.ts` that seeds sentinel rows, runs the LLM pipeline end-to-end (using Plan 02's `runProcessItem` directly, not through Trigger.dev — matching Phase 2's `verify-ingest.ts` pattern), asserts ROADMAP Success Criteria #1-4 programmatically, and prints the Langfuse dashboard URL for the human to verify SC#5. Also ship the UAT checklist document.

The harness runs against live Neon dev branch + live Anthropic + live Voyage + live Langfuse (all real API calls, not mocks). Cost per full run: ~$0.01-0.02 (2-3 Haiku calls + 2-3 Voyage embeddings + the malformed fixture run doesn't hit LLMs). Cleanup removes all sentinel rows in `finally{}` per the Phase 2 lesson (verify-ingest.ts:373-390).

Purpose: Validates ALL 20 Phase 3 requirements (LLM-01..13 + CLUST-01..07) via the 5 ROADMAP SCs. This plan's `requirements` field lists all 20 because verify:llm is the single gate that observes them in live operation.
Output: 1 harness script + 1 package.json entry + 1 UAT doc; `pnpm verify:llm` exits 0 on dev branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-llm-pipeline-clustering/03-RESEARCH.md
@.planning/phases/03-llm-pipeline-clustering/03-PATTERNS.md
@.planning/phases/03-llm-pipeline-clustering/03-VALIDATION.md
@.planning/phases/02-ingestion-pipeline/02-UAT.md
@scripts/verify-ingest.ts
@src/lib/db/schema.ts
@src/lib/llm/process-item-core.ts
@src/lib/cluster/refresh.ts
@package.json

<interfaces>
Phase 2 verify-ingest.ts (CANONICAL live-harness template — scripts/verify-ingest.ts):

  - Env bootstrap lines 23-25 (dotenv + .env.local)
  - Result-recorder lines 35-42 (record(name, pass, detail))
  - Sentinel insert/cleanup lines 44-60
  - main() returns Promise<boolean>; tail `.then(passed => process.exit(passed ? 0 : 1))` lines 393-400
  - CRITICAL lesson lines 373-376: DO NOT call process.exit() inside try — bypasses finally — leaks sentinels

runProcessItem signature (src/lib/llm/process-item-core.ts after Plan 04 wiring):

  runProcessItem(params: {
    itemId: bigint | string;
    deps?: ProcessItemDeps;
  }): Promise<ProcessItemResult>
    where ProcessItemResult = { itemId, status: 'published'|'dead_letter', clusterId?, joinedExistingCluster?, failureReason? }

runRefreshClusters signature (src/lib/cluster/refresh.ts):

  runRefreshClusters(deps?: {...}): Promise<{ updated: number }>

items schema fields for seeding (src/lib/db/schema.ts:36-74):
  sourceId (int, FK sources), url (text unique), urlFingerprint, contentHash,
  title, bodyRaw, status='pending', publishedAt, retryCount=0, ingestedAt (default now)

Haiku malformed-response fixture approach (LLM-10 → SC#3):
  Test harness does NOT hit Anthropic for SC#3. Instead, inject a stub Anthropic
  client via process-item-core's `deps.enrichWithClaude` that returns a malformed
  parsed_output (e.g., score=150 out of range). Runs the orchestrator with DI,
  asserts status='dead_letter' without burning a real API call on garbage.

SC#4 (cluster join) approach: seed two items with the SAME url pointing at similar
text content from two different source_ids. Real Haiku + real Voyage → the two
embeddings should land ≥ 0.82 cosine similarity → joinOrCreateCluster joins them
in the second call. Run runRefreshClusters() after → assert member_count >= 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft scripts/verify-llm.ts harness + register verify:llm in package.json</name>
  <files>scripts/verify-llm.ts, package.json</files>
  <read_first>
    - scripts/verify-ingest.ts EVERY LINE (canonical live-harness shape; it IS the template)
    - scripts/verify-ingest.ts:373-376 (process.exit-inside-try anti-pattern — the critical lesson)
    - scripts/verify-ingest.ts:386-390 (cleanup-in-finally — non-negotiable)
    - .planning/ROADMAP.md §"Phase 3: LLM Pipeline + Clustering" Success Criteria 1-5 (the contract)
    - 03-RESEARCH.md §Validation Architecture (lines 646-698) — per-SC test map
    - 03-PATTERNS.md §scripts/verify-llm.ts (role-match analog)
    - src/lib/llm/process-item-core.ts (Plan 02 + Plan 04 final wiring)
    - src/lib/cluster/refresh.ts (Plan 03)
    - src/lib/db/schema.ts:36-74 (items), :183-206 (pipelineRuns), :77-85 (clusters)
  </read_first>
  <action>
Create `scripts/verify-llm.ts` modeled on `scripts/verify-ingest.ts`:

    /**
     * Phase 3 verification harness — runs the LLM pipeline end-to-end against
     * a live Neon dev branch + live Anthropic + live Voyage + live Langfuse,
     * asserts ROADMAP §Phase 3 Success Criteria #1-4 programmatically, and
     * prints a Langfuse trace URL for SC#5 human UAT.
     *
     * Run: `pnpm verify:llm`
     *
     * Exits 0 on PASS, 1 on any failure. Cleans up its own sentinel rows on exit.
     * NOT run by CI — requires live ANTHROPIC_API_KEY + VOYAGE_API_KEY + DATABASE_URL
     * + (optional) LANGFUSE_*.
     *
     * Cost: ~$0.01-0.02 per full run (2 real Haiku + 2 real Voyage + 1 malformed
     * fixture that never hits APIs).
     *
     * Pattern source: scripts/verify-ingest.ts (Phase 2 template).
     */
    import 'dotenv/config';
    import { config } from 'dotenv';
    config({ path: '.env.local' });

    import { eq, inArray, sql } from 'drizzle-orm';
    import { db } from '../src/lib/db/client';
    import { items, sources, pipelineRuns, clusters } from '../src/lib/db/schema';
    import { runProcessItem } from '../src/lib/llm/process-item-core';
    import { runRefreshClusters } from '../src/lib/cluster/refresh';
    import { urlFingerprint, contentHash } from '../src/lib/ingest/fingerprint';

    const SENTINEL_PREFIX = '__verify_llm__';
    const SOURCE_A_ROUTE = `/${SENTINEL_PREFIX}/source-a`;
    const SOURCE_B_ROUTE = `/${SENTINEL_PREFIX}/source-b`;
    const SOURCE_MAL_ROUTE = `/${SENTINEL_PREFIX}/malformed`;

    type CriterionResult = { name: string; pass: boolean; detail: string };
    const results: CriterionResult[] = [];
    function record(name: string, pass: boolean, detail: string) {
      results.push({ name, pass, detail });
      console.log(`[${pass ? 'PASS' : 'FAIL'}] ${name}: ${detail}`);
    }

    async function insertSentinelSource(name: string, route: string, lang: 'en' | 'zh'): Promise<number> {
      await db
        .insert(sources)
        .values({ name, rssUrl: route, language: lang, weight: '0.1' })
        .onConflictDoNothing({ target: sources.rssUrl });
      const found = await db.select({ id: sources.id }).from(sources).where(eq(sources.rssUrl, route));
      if (!found[0]) throw new Error(`failed to insert sentinel source ${route}`);
      return found[0].id;
    }

    async function insertSentinelItem(params: {
      sourceId: number;
      urlPath: string;
      title: string;
      bodyRaw: string;
      publishedAt: Date;
    }): Promise<bigint> {
      const url = `https://example.com${params.urlPath}?sentinel=${Date.now()}-${Math.random()}`;
      const fp = urlFingerprint(url);
      const ch = contentHash(url, params.title);
      const inserted = await db
        .insert(items)
        .values({
          sourceId: params.sourceId,
          url,
          urlFingerprint: fp,
          contentHash: ch,
          title: params.title,
          bodyRaw: params.bodyRaw,
          publishedAt: params.publishedAt,
          status: 'pending',
          retryCount: 0,
        })
        .returning({ id: items.id });
      if (inserted.length === 0) throw new Error('failed to insert sentinel item');
      return inserted[0].id;
    }

    async function cleanup(sentinelSourceIds: number[]) {
      if (sentinelSourceIds.length === 0) return;
      await db.delete(pipelineRuns).where(
        sql`item_id IN (SELECT id FROM items WHERE source_id IN (${sql.join(sentinelSourceIds.map((i) => sql`${i}`), sql`, `)}))`,
      );
      // Delete clusters whose primary_item_id is a sentinel item (cascade in next step).
      const sentinelItemIds = await db
        .select({ id: items.id })
        .from(items)
        .where(inArray(items.sourceId, sentinelSourceIds));
      const clusterIds = Array.from(
        new Set(
          (
            await db
              .select({ cid: items.clusterId })
              .from(items)
              .where(inArray(items.sourceId, sentinelSourceIds))
          )
            .map((r) => r.cid)
            .filter((c): c is bigint => c !== null),
        ),
      );
      await db.delete(items).where(inArray(items.sourceId, sentinelSourceIds));
      if (clusterIds.length > 0) {
        await db.delete(clusters).where(inArray(clusters.id, clusterIds));
      }
      await db.delete(sources).where(inArray(sources.id, sentinelSourceIds));
      void sentinelItemIds; // suppress unused-var in TS strict
    }

    async function main(): Promise<boolean> {
      if (!process.env.DATABASE_URL) throw new Error('DATABASE_URL not set');
      if (!process.env.ANTHROPIC_API_KEY) throw new Error('ANTHROPIC_API_KEY not set');
      if (!process.env.VOYAGE_API_KEY) throw new Error('VOYAGE_API_KEY not set');

      console.log('== Pre-flight ==');
      const sourceAId = await insertSentinelSource('Sentinel A (verify-llm)', SOURCE_A_ROUTE, 'en');
      const sourceBId = await insertSentinelSource('Sentinel B (verify-llm)', SOURCE_B_ROUTE, 'en');
      const sourceMalId = await insertSentinelSource('Sentinel Malformed (verify-llm)', SOURCE_MAL_ROUTE, 'en');
      const sentinelSourceIds = [sourceAId, sourceBId, sourceMalId];

      try {
        const nowUtc = new Date();
        // Choose a body that's >= 500 chars so extract-full-text short-circuits (no fetch).
        // Content pattern: two items reporting the same event — SC#4 expects them to cluster.
        const sharedEventBody = `
Anthropic today announced Claude Sonnet 4.6, the latest model in the Claude 4 family.
The model introduces structured output enforcement, a 1M-token context window, and
improved reasoning for agentic workflows. Claude Sonnet 4.6 is available today via the
Claude API at $3 per million input tokens and $15 per million output tokens. The model
scores 72% on SWE-bench Verified and 48% on the GPQA Diamond benchmark.
Early-access partners including Cursor, Zed, and Replit report substantial improvements
in multi-turn tool use and code-generation consistency. The model supports prompt caching
with 5-minute and 1-hour TTLs, and is cross-compatible with the tool-use and
structured-output APIs released earlier in 2025.
        `.trim();
        const variantB = sharedEventBody.replace(
          'Anthropic today announced',
          'Anthropic has unveiled',
        ).replace(
          'the latest model',
          'their newest model',
        );

        // Sentinel A — source A, English, shared-event body
        const itemAId = await insertSentinelItem({
          sourceId: sourceAId,
          urlPath: '/anthropic-sonnet-4-6-a',
          title: 'Anthropic Launches Claude Sonnet 4.6',
          bodyRaw: sharedEventBody,
          publishedAt: new Date(nowUtc.getTime() - 60_000),
        });

        // Sentinel B — source B, English, same event, different wording + slightly later timestamp
        const itemBId = await insertSentinelItem({
          sourceId: sourceBId,
          urlPath: '/anthropic-sonnet-4-6-b',
          title: 'Claude 4.6 Unveiled by Anthropic',
          bodyRaw: variantB,
          publishedAt: nowUtc,
        });

        // Sentinel Malformed — source Malformed, content is valid but we inject
        // a malformed enrich dep at the runProcessItem call site (no real API cost).
        const malformedItemBody = 'A short article about AI stuff. Just some test content.';
        const itemMalId = await insertSentinelItem({
          sourceId: sourceMalId,
          urlPath: '/malformed-test',
          title: 'Malformed Fixture',
          bodyRaw: malformedItemBody.padEnd(600, ' filler'), // >=500 to skip extract fetch
          publishedAt: nowUtc,
        });

        // ======================================================================
        // RUN PIPELINE — SC#1 (happy) + SC#2 (cache) via real API calls
        // ======================================================================
        console.log('\n== Run: Sentinel A (first item, populates Haiku cache) ==');
        const resA = await runProcessItem({ itemId: itemAId });
        console.log(`  result: ${JSON.stringify(resA)}`);

        console.log('\n== Run: Sentinel B (second item, should cache-read from A) ==');
        const resB = await runProcessItem({ itemId: itemBId });
        console.log(`  result: ${JSON.stringify(resB)}`);

        // ======================================================================
        // SC#3 malformed fixture — inject a broken enrich that returns a zod-violating parsed_output.
        // ======================================================================
        console.log('\n== Run: Sentinel Malformed (expected dead_letter via DI) ==');
        const resMal = await runProcessItem({
          itemId: itemMalId,
          deps: {
            // Force a ZodError by returning out-of-bounds score.
            enrichWithClaude: async () => {
              // This will fail EnrichmentSchema.parse inside enrich.ts if enrich.ts still parses,
              // OR fail process-item-core's zod validate. Either way → dead_letter.
              const bad = {
                enrichment: {
                  title_zh: '',  // violates min(1)
                  summary_zh: 'x',  // violates min(10)
                  score: 150,  // violates max(100)
                  recommendation: '',  // violates min(2)
                  tags: [],  // violates min(1)
                },
                usage: {
                  input_tokens: 100, output_tokens: 20,
                  cache_read_input_tokens: 0, cache_creation_input_tokens: 0,
                },
                latencyMs: 1,
              };
              throw new (await import('zod')).ZodError([
                { code: 'custom', path: ['score'], message: 'sentinel-malformed' },
              ]);
              // eslint-disable-next-line no-unreachable
              return bad as never;
            },
          },
        });
        console.log(`  result: ${JSON.stringify(resMal)}`);

        // ======================================================================
        // RUN REFRESH-CLUSTERS — required for SC#4 member_count assertion
        // ======================================================================
        console.log('\n== Run: runRefreshClusters ==');
        const refResult = await runRefreshClusters();
        console.log(`  updated: ${refResult.updated}`);

        // ======================================================================
        // ASSERTIONS
        // ======================================================================
        console.log('\n== Assertions ==');

        // ---------- SC#1: enrichment fields on A ----------
        const aRow = await db.select().from(items).where(eq(items.id, itemAId));
        const a = aRow[0];
        const sc1Ok =
          a.status === 'published' &&
          !!a.titleZh && a.titleZh.length > 0 &&
          !!a.summaryZh && a.summaryZh.length > 0 &&
          typeof a.score === 'number' && a.score >= 0 && a.score <= 100 &&
          !!a.recommendation && a.recommendation.length > 0 &&
          Array.isArray(a.tags) && (a.tags?.length ?? 0) >= 1;
        record(
          'SC#1 enrichment fields populated',
          sc1Ok,
          `status=${a.status}, title_zh_len=${a.titleZh?.length}, score=${a.score}, tags=${JSON.stringify(a.tags)}`,
        );

        // ---------- SC#2: cache_read_input_tokens > 0 on at least one pipeline_runs row ----------
        const cacheRes = (await db.execute(sql`
          SELECT MAX(cache_read_tokens)::int AS max_cache FROM pipeline_runs
          WHERE item_id IN (${itemAId}, ${itemBId}) AND task = 'enrich'
        `)) as unknown as { rows: Array<{ max_cache: number | null }> };
        const maxCache = cacheRes.rows[0]?.max_cache ?? 0;
        record(
          'SC#2 prompt caching active',
          (maxCache ?? 0) > 0,
          `MAX(cache_read_tokens)=${maxCache} across sentinel A+B enrich runs`,
        );

        // ---------- SC#3: malformed → dead_letter ----------
        const malRow = await db.select().from(items).where(eq(items.id, itemMalId));
        const m = malRow[0];
        const sc3Ok = m.status === 'dead_letter' && !!m.failureReason;
        record(
          'SC#3 malformed → dead_letter (not published)',
          sc3Ok,
          `status=${m.status}, failure_reason=${m.failureReason}`,
        );

        // ---------- SC#4: two items cluster together + member_count >= 2 ----------
        const aFresh = await db.select().from(items).where(eq(items.id, itemAId));
        const bFresh = await db.select().from(items).where(eq(items.id, itemBId));
        const sameCluster =
          aFresh[0].clusterId !== null &&
          bFresh[0].clusterId !== null &&
          aFresh[0].clusterId === bFresh[0].clusterId;
        let sc4Ok = sameCluster;
        let sc4Detail = `A.cluster_id=${aFresh[0].clusterId}, B.cluster_id=${bFresh[0].clusterId}`;
        if (sameCluster && aFresh[0].clusterId) {
          const cluster = await db.select().from(clusters).where(eq(clusters.id, aFresh[0].clusterId));
          const memberCount = cluster[0]?.memberCount ?? 0;
          const primaryOk = cluster[0]?.primaryItemId === itemAId; // A had earlier published_at
          sc4Ok = sc4Ok && memberCount >= 2 && primaryOk;
          sc4Detail += `, member_count=${memberCount}, primary=${cluster[0]?.primaryItemId} (expected ${itemAId})`;
        } else if (aFresh[0].clusterId && bFresh[0].clusterId) {
          sc4Detail += ' — items landed in DIFFERENT clusters. Possible causes: cosine<0.82 (tune threshold) or HNSW index missing (run pnpm check:hnsw).';
        }
        record('SC#4 cross-source clustering', sc4Ok, sc4Detail);

        // ---------- SC#5: Langfuse trace presence (manual UAT) ----------
        const langfuseBase = process.env.LANGFUSE_BASE_URL ?? 'https://cloud.langfuse.com';
        console.log(`\n== SC#5 (MANUAL UAT) ==`);
        console.log(`  Open Langfuse dashboard: ${langfuseBase}`);
        console.log(`  Look for traces with timestamp within the last 5 minutes.`);
        console.log(`  Expected: 2 successful Haiku traces (items ${itemAId}, ${itemBId}) + 2 Voyage embed spans.`);
        console.log(`  Each trace should show non-zero input_tokens + non-zero estimated cost.`);
        console.log(`  Record the pass/fail decision in 03-UAT.md.`);

        // Summary
        console.log('\n== Summary (SC#1-4 auto; SC#5 manual) ==');
        for (const r of results) console.log(`  [${r.pass ? 'PASS' : 'FAIL'}] ${r.name}`);
        const failed = results.filter((r) => !r.pass);
        if (failed.length > 0) {
          console.error(`\n${failed.length}/${results.length} automated criteria FAILED. SC#5 still requires manual UAT.`);
        } else {
          console.log(`\nAll ${results.length} automated criteria PASSED. SC#5 requires manual Langfuse UAT.`);
        }
        return failed.length === 0;
      } finally {
        console.log('\n== Cleanup ==');
        await cleanup(sentinelSourceIds);
        console.log(`Removed sentinel sources ${sentinelSourceIds.join(', ')} + their items + pipeline_runs + clusters.`);
      }
    }

    main()
      .then((passed) => {
        process.exit(passed ? 0 : 1);
      })
      .catch((e) => {
        console.error('VERIFY FAILED:', e);
        process.exit(1);
      });

Register in `package.json` — add AFTER existing verify:ingest:

    "verify:llm": "tsx --env-file=.env.local scripts/verify-llm.ts"
  </action>
  <verify>
    <automated>test -f scripts/verify-llm.ts &amp;&amp; grep -q "verify:llm" package.json &amp;&amp; grep -q "runProcessItem" scripts/verify-llm.ts &amp;&amp; grep -q "runRefreshClusters" scripts/verify-llm.ts &amp;&amp; grep -q "cache_read_tokens" scripts/verify-llm.ts &amp;&amp; grep -q "dead_letter" scripts/verify-llm.ts &amp;&amp; grep -q "finally" scripts/verify-llm.ts &amp;&amp; grep -q "main().then" scripts/verify-llm.ts &amp;&amp; grep -q "ZodError" scripts/verify-llm.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-llm.ts` exists and typechecks
    - Grep `runProcessItem` present (pipeline call)
    - Grep `runRefreshClusters` present (SC#4 member_count precondition)
    - Grep `cache_read_tokens` present (SC#2)
    - Grep `dead_letter` present (SC#3)
    - Grep `finally` present AND `cleanup(` call is inside a finally block (per verify-ingest.ts:386-390 lesson)
    - Grep `main().then` present at file tail — process.exit is OUTSIDE the try (per verify-ingest.ts:373-376 lesson)
    - `process.exit` does NOT appear inside `async function main` body — only in the `.then`/`.catch` tail
    - Grep `ZodError` present in the SC#3 malformed DI (injected schema failure, no real API cost)
    - `package.json` has `"verify:llm": "tsx --env-file=.env.local scripts/verify-llm.ts"`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>verify:llm harness ready; registered in package.json; typecheck green. Script is ready for the human-gated live run in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Draft 03-UAT.md human checklist with exact verification steps for each SC</name>
  <files>.planning/phases/03-llm-pipeline-clustering/03-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 3: LLM Pipeline + Clustering" — Goal + 5 Success Criteria (verbatim source)
    - .planning/phases/02-ingestion-pipeline/02-UAT.md (canonical UAT doc shape — Phase 2 precedent)
    - 03-VALIDATION.md §Manual-Only Verifications (pre-drafted per-SC notes)
    - scripts/verify-llm.ts (from Task 1 — UAT doc references the exact output assertions)
  </read_first>
  <action>
Create `.planning/phases/03-llm-pipeline-clustering/03-UAT.md` — mirror the Phase 2 UAT shape + include per-SC verification steps:

    ---
    phase: 03-llm-pipeline-clustering
    created: 2026-04-21
    status: pending
    ---

    # Phase 3 — User Acceptance Test

    Gated by the orchestrator after Plan 05 ships. Confirms all 5 ROADMAP Success Criteria live.

    ## Preflight

    - [ ] `.env.local` has `DATABASE_URL` pointing at a Neon DEV branch (never production)
    - [ ] `.env.local` has `ANTHROPIC_API_KEY` + `VOYAGE_API_KEY` + `LANGFUSE_*` (real credentials; LANGFUSE optional if UAT SC#5 is deferred)
    - [ ] `pnpm check:hnsw` exits 0 (Plan 01 applied on the dev branch)
    - [ ] `pnpm test --run` exits 0 (library tests all green)
    - [ ] `pnpm typecheck` exits 0

    ## Automated Run

    Command: `pnpm verify:llm`

    Expected cost: ~$0.01-0.02 (2 real Haiku calls + 2 real Voyage calls; SC#3 is DI-injected, no API cost)
    Expected duration: 30-60 seconds.

    Expected stdout PASS lines:
      - `[PASS] SC#1 enrichment fields populated`
      - `[PASS] SC#2 prompt caching active`
      - `[PASS] SC#3 malformed → dead_letter (not published)`
      - `[PASS] SC#4 cross-source clustering`

    If any FAIL, see troubleshooting section below.

    ## Per-SC Checklist

    ### SC#1 — English source item → full Chinese enrichment
    - [ ] `pnpm verify:llm` output shows `[PASS] SC#1`
    - [ ] Open Neon SQL editor → `SELECT title_zh, summary_zh, score, recommendation, tags FROM items WHERE source_id IN (SELECT id FROM sources WHERE name LIKE '%Sentinel A%') ORDER BY ingested_at DESC LIMIT 1;` — confirm a Chinese title + 2-4 sentence Chinese summary + score in [0,100] + Chinese 推荐理由 + ≥1 tag
    - [ ] (Manual linguistic) Chinese output reads naturally (no English leakage in title_zh/summary_zh/recommendation) — NOTE: cleanup deletes the row after verify:llm exits, so this check must happen DURING the verify run (suspend with Ctrl+Z or save the SELECT results)
    - Reference: .planning/ROADMAP.md Phase 3 SC#1

    ### SC#2 — Prompt caching active (cache_read_input_tokens > 0)
    - [ ] `pnpm verify:llm` output shows `[PASS] SC#2` with `MAX(cache_read_tokens) > 0`
    - [ ] If FAIL: Check that the system prompt in `src/lib/llm/prompt.ts` renders to ≥ 4096 tokens (RESEARCH.md Pitfall 1). Run `pnpm test --run src/lib/llm/prompt` — the token-floor test MUST be green.
    - [ ] If FAIL: Confirm both enrich calls happened within 5 minutes (verify:llm runs them sequentially — default is fine).
    - Reference: .planning/ROADMAP.md Phase 3 SC#2

    ### SC#3 — Malformed LLM response → dead_letter
    - [ ] `pnpm verify:llm` output shows `[PASS] SC#3` with status=dead_letter + failure_reason non-empty
    - [ ] failure_reason is scrubbed (no API key fragments, no URL fragments). Inspect verify:llm console output for the SC#3 detail line.
    - Reference: .planning/ROADMAP.md Phase 3 SC#3

    ### SC#4 — Cross-source clustering
    - [ ] `pnpm verify:llm` output shows `[PASS] SC#4` with member_count >= 2 and primary_item_id = sentinel A's id
    - [ ] If FAIL with "items landed in DIFFERENT clusters": the Voyage embedding similarity is below threshold. Options: (a) confirm Plan 01 HNSW index present via `pnpm check:hnsw`; (b) lower threshold via `UPDATE settings SET value='0.75' WHERE key='cluster_threshold';` temporarily, re-run, then restore; (c) check that `shared_event_body` in verify-llm.ts is semantically similar enough — if Haiku's two Chinese summaries drift too far, embeddings may diverge.
    - Reference: .planning/ROADMAP.md Phase 3 SC#4

    ### SC#5 — Langfuse trace per item with cost breakdown (MANUAL)
    - [ ] Open `${LANGFUSE_BASE_URL}` dashboard (default https://cloud.langfuse.com)
    - [ ] Navigate to Traces → filter by the last 10 minutes
    - [ ] Confirm ≥ 2 traces exist matching the verify:llm run (each labeled with an Anthropic messages.parse call)
    - [ ] Open the most recent trace → confirm the following fields are populated:
        - input_tokens > 0
        - output_tokens > 0
        - cache_read_input_tokens > 0 on the SECOND trace (not the first)
        - estimated cost in USD is shown (non-zero)
        - No `sk-ant-...` prefix appears in prompt/completion text (privacy check)
    - [ ] Check Voyage embedding spans (if OpenInference instruments voyageai — may or may not be captured depending on A2 verification outcome in Plan 02)
    - [ ] If Langfuse credentials are NOT configured, record SC#5 as **DEFERRED** (like Phase 2 SC#2 was) and unblock the phase after confirming admin-observability via `pipeline_runs` table instead:
      `SELECT item_id, model, task, input_tokens, cache_read_tokens, estimated_cost_usd FROM pipeline_runs ORDER BY created_at DESC LIMIT 6;`
    - Reference: .planning/ROADMAP.md Phase 3 SC#5

    ## Troubleshooting

    | Symptom | Likely Cause | Fix |
    |---------|--------------|-----|
    | SC#1 FAIL: `status=dead_letter` on sentinel A | Haiku schema-validation failure | Read `items.failure_reason`. If ZodError, check EnrichmentSchema bounds or Haiku output quality; if network error, re-run. |
    | SC#2 FAIL: `MAX(cache_read_tokens)=0` | System prompt < 4096 tokens (Pitfall 1) | Run prompt.test.ts — must assert `estimateTokens >= 4096`. Add more rubric/taxonomy content to prompt.ts. |
    | SC#3 FAIL: `status=published` on malformed fixture | `runProcessItem` didn't classify ZodError as terminal | Review `process-item-core.ts` ERROR PATH — ZodError must be `isTerminal=true`. |
    | SC#4 FAIL: different clusters | Threshold too strict OR HNSW missing | (a) `pnpm check:hnsw`; (b) `SELECT value FROM settings WHERE key='cluster_threshold';` — should be '0.82' |
    | SC#4 FAIL: same cluster but `member_count=1` | `runRefreshClusters` didn't find the cluster dirty | Check verify-llm.ts — runRefreshClusters() MUST be called AFTER both runProcessItem calls. |
    | SC#5 DEFERRED | Langfuse env not set | Acceptable — note in PR. Phase 3 exit criteria allow OPS/observability deferral matching Phase 2 precedent. |

    ## Sign-off

    - [ ] SC#1 PASS
    - [ ] SC#2 PASS
    - [ ] SC#3 PASS
    - [ ] SC#4 PASS
    - [ ] SC#5 PASS (or explicitly DEFERRED with `pipeline_runs` fallback observed)

    **Verifier:** {name}
    **Date:** {YYYY-MM-DD}
    **Dev branch:** {neon-branch-name-or-URL}
    **Verdict:** ⬜ PASS · ⬜ FAIL · ⬜ DEFERRED (SC#5 only)
  </action>
  <verify>
    <automated>test -f .planning/phases/03-llm-pipeline-clustering/03-UAT.md &amp;&amp; grep -q "SC#1" .planning/phases/03-llm-pipeline-clustering/03-UAT.md &amp;&amp; grep -q "SC#5" .planning/phases/03-llm-pipeline-clustering/03-UAT.md &amp;&amp; grep -q "Langfuse" .planning/phases/03-llm-pipeline-clustering/03-UAT.md &amp;&amp; grep -q "pnpm verify:llm" .planning/phases/03-llm-pipeline-clustering/03-UAT.md &amp;&amp; grep -q "cluster_threshold" .planning/phases/03-llm-pipeline-clustering/03-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - UAT file exists at the expected path
    - Contains sections for SC#1 through SC#5
    - References `pnpm verify:llm` (Task 1's script) + `pnpm check:hnsw` (Plan 01's script)
    - Explicitly calls out SC#5 deferral protocol matching Phase 2 precedent
    - Troubleshooting section has specific remediation commands for each SC failure mode
  </acceptance_criteria>
  <done>UAT doc ready for human verifier post-live-run; captures exact SQL queries + Langfuse dashboard navigation + troubleshooting table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [HUMAN UAT] Run pnpm verify:llm on the live dev branch and complete 03-UAT.md sign-off</name>
  <what-built>
    Task 1 shipped `scripts/verify-llm.ts` + registered `pnpm verify:llm`. Task 2 shipped `03-UAT.md`. All the plumbing from Plans 01-04 is in place (migration applied, LLM core + cluster libs + Trigger.dev tasks wired). Nothing has actually RUN end-to-end against live APIs yet.
  </what-built>
  <how-to-verify>
    Follow the checklist in `.planning/phases/03-llm-pipeline-clustering/03-UAT.md` exactly:

    1. Preflight section — confirm all 5 items checked (env, HNSW, tests green, typecheck)

    2. Run `pnpm verify:llm` against the Neon DEV branch. Expected: ~$0.01-0.02 API spend, 30-60s duration, exit code 0.

    3. Inspect stdout for four [PASS] lines covering SC#1, SC#2, SC#3, SC#4. Any FAIL → follow the Troubleshooting table (do NOT proceed).

    4. While verify:llm is mid-run (or by adding a sleep/pause before cleanup for inspection), capture the sentinel A row's Chinese output for linguistic UAT:
       `SELECT title_zh, summary_zh, recommendation FROM items WHERE source_id IN (SELECT id FROM sources WHERE name LIKE '%Sentinel A%');`
       Confirm: reads as natural Chinese, no English leakage.

    5. For SC#5: open Langfuse dashboard → confirm 2+ traces with non-zero cost and cache_read_input_tokens on the second trace. If Langfuse env is not configured, mark SC#5 as DEFERRED and verify `pipeline_runs` has token data (fallback pathway documented in UAT).

    6. Fill in 03-UAT.md sign-off: name, date, dev branch, verdict. Commit the filled-in UAT doc.

    7. Type "approved" to close the phase (or describe any failures — a fail triggers /gsd-plan-phase --gaps in the orchestrator).
  </how-to-verify>
  <files>.planning/phases/03-llm-pipeline-clustering/03-UAT.md (human fills sign-off; no code change)</files>
  <action>
Human verifier follows the 03-UAT.md checklist step-by-step. See <how-to-verify> block above. Executes pnpm verify:llm against the live dev branch, inspects stdout for four [PASS] lines, performs SC#5 Langfuse dashboard check (or records DEFERRED), fills sign-off block in 03-UAT.md, commits it.
  </action>
  <verify>
    <automated>pnpm verify:llm</automated>
  </verify>
  <done>pnpm verify:llm exits 0 (SC#1-4 automated PASS) AND 03-UAT.md sign-off filled with SC#5 = PASS or DEFERRED AND human types "approved".</done>
  <resume-signal>Type "approved" after all 5 SCs PASS (or SC#5 explicitly DEFERRED) and 03-UAT.md is signed + committed. If any failure, describe it — orchestrator will route to --gaps replan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify-llm.ts → live APIs | Runs real Anthropic + Voyage calls against live credentials (burns $0.01-0.02 per run) |
| verify-llm.ts → dev DB | Writes sentinel rows; cleans up in finally{} |
| UAT human inspector → Langfuse dashboard | SC#5 depends on trace visibility — private dashboard gated by auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Info Disclosure (sentinel data persistence) | `verify-llm.ts` cleanup | mitigate | All sentinel rows deleted in `finally{}` regardless of assertion outcome — identical pattern to verify-ingest.ts:386-390. Sentinel URLs include a random token so re-runs don't collide with prior runs' fingerprint UNIQUE index. |
| T-03-24 | Info Disclosure (verify output in CI logs) | `verify:llm` stdout | accept | verify:llm is NOT run by CI (package.json script is dev-only). Developer local run is interactive. Langfuse dashboard URLs printed contain no secrets. |
| T-03-25 | Cost overrun (accidental re-run) | `verify:llm` | mitigate | Per-run cost cap ~$0.02 (2 Haiku + 2 Voyage calls, SC#3 is DI-injected). No loop, no retry amplification. Documented in UAT preflight + harness JSDoc. |
| T-03-26 | Production corruption (running against prod branch) | `verify:llm` | mitigate | UAT preflight REQUIRES operator to confirm DATABASE_URL points at dev branch. The harness does not self-protect against a misconfigured prod URL — rely on the documented Phase 1 D-11 branch-per-PR discipline. For v1 this is acceptable; admin-owned prod ops are Phase 6. |
</threat_model>

<verification>
- `pnpm typecheck` green
- `scripts/verify-llm.ts` exists and follows verify-ingest.ts shape (process.exit outside try, cleanup in finally)
- `pnpm verify:llm` command registered
- `03-UAT.md` lists all 5 SCs + preflight + troubleshooting
- Task 3 is a blocking human gate — not automation
</verification>

<success_criteria>
Plan 05 DONE when:

1. `scripts/verify-llm.ts` shipped + registered in package.json
2. `03-UAT.md` drafted with per-SC checklist + troubleshooting
3. Human verifier runs `pnpm verify:llm` on dev branch with 4 [PASS] results AND completes SC#5 (or DEFERRED with fallback)
4. 03-UAT.md filled + signed + committed
5. Verdict = PASS (or PASS with SC#5 DEFERRED)

If verdict = FAIL, the orchestrator routes to `/gsd-plan-phase 03 --gaps` (Phase 2 precedent).
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-pipeline-clustering/03-05-SUMMARY.md` recording:
- verify:llm run outcomes (4 automated PASS/FAIL lines)
- SC#5 final state (PASS or DEFERRED + reason)
- Langfuse trace URLs captured (if PASS)
- Any quirks with sentinel cleanup (Cluster rows cascading correctly?)
- Actual API spend (approximate, from Langfuse cost aggregate or Anthropic dashboard)
</output>
