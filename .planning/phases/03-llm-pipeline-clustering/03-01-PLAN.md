---
phase: 03-llm-pipeline-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0003_hnsw_index_and_settings_seed.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0003_snapshot.json
  - scripts/check-hnsw.ts
  - package.json
  - .env.example
  - vitest.setup.ts
autonomous: false
requirements: [CLUST-02, CLUST-04]
tags: [migration, pgvector, hnsw, settings, env]

must_haves:
  truths:
    - "An HNSW index named items_embedding_hnsw_idx exists on items.embedding with vector_cosine_ops and WITH (m=16, ef_construction=64)"
    - "Settings table contains row where key='cluster_threshold' and value='0.82'"
    - ".env.example documents ANTHROPIC_API_KEY, VOYAGE_API_KEY, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_BASE_URL"
    - "pnpm check:hnsw exits 0 against live dev branch"
  artifacts:
    - path: "drizzle/0003_hnsw_index_and_settings_seed.sql"
      provides: "HNSW index creation + settings threshold seed (idempotent)"
      contains: "CREATE INDEX IF NOT EXISTS items_embedding_hnsw_idx"
    - path: "drizzle/meta/_journal.json"
      provides: "Drizzle migration journal entry idx=3"
      contains: "0003_hnsw_index_and_settings_seed"
    - path: "drizzle/meta/0003_snapshot.json"
      provides: "Drizzle snapshot for migration 0003 (copy-forward of 0002 — schema DSL unchanged)"
    - path: "scripts/check-hnsw.ts"
      provides: "Post-push CLI asserting HNSW index present on items.embedding"
      contains: "items_embedding_hnsw_idx"
    - path: "package.json"
      provides: "check:hnsw and verify:hnsw scripts using tsx --env-file=.env.local"
      contains: "check:hnsw"
    - path: ".env.example"
      provides: "Phase 3 env vars registry (ANTHROPIC/VOYAGE/LANGFUSE)"
      contains: "LANGFUSE_PUBLIC_KEY"
    - path: "vitest.setup.ts"
      provides: "Dummy env vars for module-load of @/lib/llm/client in tests"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "drizzle/0003_hnsw_index_and_settings_seed.sql"
      to: "items.embedding column"
      via: "CREATE INDEX USING hnsw (embedding vector_cosine_ops)"
      pattern: "hnsw \\(embedding vector_cosine_ops\\)"
    - from: "scripts/check-hnsw.ts"
      to: "pg_indexes catalog"
      via: "SELECT indexname FROM pg_indexes WHERE tablename='items'"
      pattern: "items_embedding_hnsw_idx"
---

<objective>
Ship the Phase 3 schema migration (HNSW index on items.embedding + settings seed for cluster_threshold) and env-topology preconditions (.env.example additions, vitest.setup.ts dummies, check:hnsw assertion script). This is the hard gate that must pass on the live dev branch before Plans 02/03 can execute — their clustering SQL depends on the HNSW index existing, and their module-load-time imports depend on ANTHROPIC_API_KEY / VOYAGE_API_KEY / LANGFUSE_* being resolvable (real in Trigger.dev Cloud, dummies in vitest.setup.ts).

Purpose: Satisfies CLUST-02 (HNSW index) + CLUST-04 (threshold in settings). Unblocks Wave 2 (Plans 02 + 03) which run in parallel.
Output: Migration 0003 applied to dev branch; threshold row seeded; env registry canonicalized; HNSW assertion script available as `pnpm check:hnsw`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-llm-pipeline-clustering/03-RESEARCH.md
@.planning/phases/03-llm-pipeline-clustering/03-PATTERNS.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/02-ingestion-pipeline/02-CONTEXT.md
@drizzle/0000_enable_pgvector.sql
@drizzle/meta/_journal.json
@src/lib/db/schema.ts
@scripts/verify-ingest.ts
@package.json
@vitest.setup.ts

<interfaces>
Hand-authored migration precedent (drizzle/0000_enable_pgvector.sql — the ONLY hand-authored migration in repo):

    CREATE EXTENSION IF NOT EXISTS vector;

Drizzle journal entry shape (drizzle/meta/_journal.json entries 0, 1, 2):

    { "idx": 2, "version": "7", "when": 1745170000000, "tag": "0002_add_published_at_source_tz", "breakpoints": true }

items.embedding column definition (src/lib/db/schema.ts line 54):

    embedding: vector('embedding', { dimensions: 1024 })  // Voyage voyage-3.5, 1024-dim

settings table (src/lib/db/schema.ts lines 176-180):

    export const settings = pgTable('settings', {
      key: text('key').primaryKey(),
      value: text('value').notNull(),
      updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
    });

check-hnsw probe pattern (mirror scripts/verify-ingest.ts:361-369 + verify-ingest.ts:393-400 exit shape):

    const res = (await db.execute(sql`
      SELECT indexname, indexdef FROM pg_indexes
      WHERE tablename = 'items' AND indexname = 'items_embedding_hnsw_idx'
    `)) as unknown as { rows: Array<{ indexname: string; indexdef: string }> };

Phase 2 script registration precedent (package.json:24-25):

    "db:seed": "tsx --env-file=.env.local drizzle/seed-sources.ts",
    "verify:ingest": "tsx --env-file=.env.local scripts/verify-ingest.ts",

vitest.setup.ts pattern — the ??= idiom (existing line 10 sets DATABASE_URL this way):

    process.env.DATABASE_URL ??= 'postgres://test:test@localhost:5432/test';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author hand-SQL migration + journal entry + snapshot copy-forward</name>
  <files>drizzle/0003_hnsw_index_and_settings_seed.sql, drizzle/meta/_journal.json, drizzle/meta/0003_snapshot.json</files>
  <read_first>
    - drizzle/0000_enable_pgvector.sql (the ONE hand-authored migration precedent)
    - drizzle/meta/_journal.json (existing entries 0, 1, 2 for exact shape)
    - drizzle/meta/0002_snapshot.json (copy-forward source — schema DSL unchanged this migration)
    - src/lib/db/schema.ts lines 36-74 (items table) + lines 176-180 (settings table)
    - 03-RESEARCH.md §HNSW index migration (lines 554-567) + §Pitfall 5
    - 03-PATTERNS.md §drizzle/0003_hnsw_index_and_settings_seed.sql (exact-analog section)
  </read_first>
  <action>
Create `drizzle/0003_hnsw_index_and_settings_seed.sql` with EXACTLY this content (hand-authored because Drizzle schema DSL does not yet emit HNSW+WITH syntax — precedent: 0000_enable_pgvector.sql):

    -- Phase 3 migration — adds HNSW index on items.embedding (CLUST-02) and seeds
    -- the cluster threshold setting (CLUST-04).
    -- Hand-authored (not drizzle-kit generated) because Drizzle's index DSL does not
    -- yet emit HNSW + vector_cosine_ops + WITH (m, ef_construction). Mirrors the
    -- precedent in 0000_enable_pgvector.sql.
    -- Source: 03-RESEARCH.md §Pattern 2 + §Pitfall 5, neon.com/docs/extensions/pgvector

    CREATE INDEX IF NOT EXISTS items_embedding_hnsw_idx
      ON items USING hnsw (embedding vector_cosine_ops)
      WITH (m = 16, ef_construction = 64);

    INSERT INTO settings (key, value)
    VALUES ('cluster_threshold', '0.82')
    ON CONFLICT (key) DO NOTHING;

Append to `drizzle/meta/_journal.json` entries[] array (preserve existing entries 0, 1, 2):

    { "idx": 3, "version": "7", "when": <Date.now() epoch ms at authoring time>, "tag": "0003_hnsw_index_and_settings_seed", "breakpoints": true }

Use `breakpoints: true`, `version: "7"` (matches entries 0, 1, 2). `when`: use current epoch ms.

Create `drizzle/meta/0003_snapshot.json` as a verbatim byte-for-byte copy of `drizzle/meta/0002_snapshot.json` — the Drizzle DSL schema (`src/lib/db/schema.ts`) is UNCHANGED by this migration (no new columns, tables, or Drizzle-representable indexes; HNSW index lives outside the DSL). This preserves drizzle-kit's chain integrity per PATTERNS.md §Shared Pattern 8.

IMPORTANT: Do NOT run `drizzle-kit generate` — it would attempt to diff schema.ts against 0002 and may rewrite 0003 or delete the hand-authored SQL. The journal + snapshot must be edited by hand.
  </action>
  <verify>
    <automated>test -f drizzle/0003_hnsw_index_and_settings_seed.sql &amp;&amp; grep -q "items_embedding_hnsw_idx" drizzle/0003_hnsw_index_and_settings_seed.sql &amp;&amp; grep -q "cluster_threshold" drizzle/0003_hnsw_index_and_settings_seed.sql &amp;&amp; grep -q "\"idx\": 3" drizzle/meta/_journal.json &amp;&amp; grep -q "0003_hnsw_index_and_settings_seed" drizzle/meta/_journal.json &amp;&amp; test -f drizzle/meta/0003_snapshot.json &amp;&amp; diff drizzle/meta/0002_snapshot.json drizzle/meta/0003_snapshot.json</automated>
  </verify>
  <acceptance_criteria>
    - File `drizzle/0003_hnsw_index_and_settings_seed.sql` exists
    - Grep: `CREATE INDEX IF NOT EXISTS items_embedding_hnsw_idx` present in the SQL
    - Grep: `USING hnsw (embedding vector_cosine_ops)` present
    - Grep: `WITH (m = 16, ef_construction = 64)` present
    - Grep: `INSERT INTO settings (key, value)` with `'cluster_threshold'` and `'0.82'` present
    - Grep: `ON CONFLICT (key) DO NOTHING` present
    - `drizzle/meta/_journal.json` contains an entry with `"idx": 3` AND `"tag": "0003_hnsw_index_and_settings_seed"` AND `"breakpoints": true` AND `"version": "7"`
    - `drizzle/meta/0003_snapshot.json` exists and is byte-identical to `drizzle/meta/0002_snapshot.json` (diff produces zero output)
  </acceptance_criteria>
  <done>Migration SQL file + journal entry + snapshot copy-forward all present; pnpm typecheck still green (no schema.ts changes).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add check-hnsw script + package.json scripts + .env.example + vitest.setup.ts</name>
  <files>scripts/check-hnsw.ts, package.json, .env.example, vitest.setup.ts</files>
  <read_first>
    - scripts/verify-ingest.ts lines 23-25 (dotenv double-belt pattern)
    - scripts/verify-ingest.ts lines 361-369 (information_schema / pg_indexes probe idiom)
    - scripts/verify-ingest.ts lines 393-400 (main().then(ok => process.exit(ok ? 0 : 1)))
    - package.json lines 24-25 (tsx --env-file=.env.local precedent — db:seed, verify:ingest)
    - vitest.setup.ts (??= idiom for env pre-population at module-load; existing DATABASE_URL line)
    - 03-PATTERNS.md §scripts/check-hnsw.ts + §.env.example (modify — new vars)
    - 03-RESEARCH.md §Environment Availability (lines 628-645)
  </read_first>
  <action>
(a) Create `scripts/check-hnsw.ts` — mirrors `scripts/verify-ingest.ts:23-25, 361-369, 393-400`:

    /**
     * Post-migration assertion — Phase 3 CLUST-02.
     *
     * Asserts that drizzle/0003_hnsw_index_and_settings_seed.sql has been applied
     * against the connected Neon branch: items_embedding_hnsw_idx exists, is an
     * HNSW index, and covers the embedding column. Also verifies settings seed.
     *
     * Run: `pnpm check:hnsw`
     *
     * Exits 0 on PASS, 1 on FAIL. NOT run by CI — developer/operator tool.
     *
     * Pattern mirrors scripts/verify-ingest.ts:361-369 (information_schema probe) +
     * verify-ingest.ts:393-400 (main().then(process.exit) shape).
     */
    import 'dotenv/config';
    import { config } from 'dotenv';
    config({ path: '.env.local' });

    import { sql } from 'drizzle-orm';
    import { db } from '../src/lib/db/client';

    async function main(): Promise<boolean> {
      if (!process.env.DATABASE_URL) throw new Error('DATABASE_URL not set');

      const res = (await db.execute(sql`
        SELECT indexname, indexdef FROM pg_indexes
        WHERE tablename = 'items' AND indexname = 'items_embedding_hnsw_idx'
      `)) as unknown as { rows: Array<{ indexname: string; indexdef: string }> };
      const rows = res.rows;

      if (rows.length !== 1) {
        console.error(`[FAIL] Expected 1 row for items_embedding_hnsw_idx, got ${rows.length}`);
        return false;
      }
      const def = rows[0].indexdef.toLowerCase();
      if (!def.includes('using hnsw')) {
        console.error(`[FAIL] Index is not HNSW: ${rows[0].indexdef}`);
        return false;
      }
      if (!def.includes('embedding')) {
        console.error(`[FAIL] Index does not cover embedding column: ${rows[0].indexdef}`);
        return false;
      }

      const sres = (await db.execute(sql`
        SELECT key, value FROM settings WHERE key = 'cluster_threshold'
      `)) as unknown as { rows: Array<{ key: string; value: string }> };
      if (sres.rows.length !== 1 || sres.rows[0].value !== '0.82') {
        console.error(`[FAIL] settings.cluster_threshold not seeded: ${JSON.stringify(sres.rows)}`);
        return false;
      }

      console.log('[PASS] items_embedding_hnsw_idx (HNSW) + settings.cluster_threshold=0.82');
      return true;
    }

    main()
      .then((ok) => { process.exit(ok ? 0 : 1); })
      .catch((e) => { console.error('check:hnsw FAILED:', e); process.exit(1); });

(b) Edit `package.json` scripts block — insert new scripts AFTER the existing `"verify:ingest"` line (preserve all existing scripts):

    "check:hnsw": "tsx --env-file=.env.local scripts/check-hnsw.ts",
    "verify:hnsw": "tsx --env-file=.env.local scripts/check-hnsw.ts"

Both aliases point to the same file (orchestrator accepts either name).

(c) Edit `.env.example` — append at end (do not overwrite existing vars):

    # Phase 3 — LLM Pipeline + Clustering
    ANTHROPIC_API_KEY=        # Haiku 4.5 structured-output calls (LLM-03..07). Set in Vercel + Trigger.dev Cloud (Phase 1 D-07).
    VOYAGE_API_KEY=           # voyage-3.5 embeddings (CLUST-01). Set in Vercel + Trigger.dev Cloud.
    LANGFUSE_PUBLIC_KEY=      # Langfuse OTel trace export (LLM-13).
    LANGFUSE_SECRET_KEY=      # Langfuse OTel trace export (LLM-13).
    LANGFUSE_BASE_URL=        # e.g., https://cloud.langfuse.com — Langfuse OTel endpoint (LLM-13).

(d) Edit `vitest.setup.ts` — append AFTER the existing `DATABASE_URL ??=` line (preserve that line). Use the same `??=` idiom:

    process.env.ANTHROPIC_API_KEY ??= 'sk-ant-test-dummy';
    process.env.VOYAGE_API_KEY ??= 'pa-test-dummy';
    process.env.LANGFUSE_PUBLIC_KEY ??= 'pk-lf-test-dummy';
    process.env.LANGFUSE_SECRET_KEY ??= 'sk-lf-test-dummy';
    process.env.LANGFUSE_BASE_URL ??= 'https://cloud.langfuse.com';

These dummies exist solely so `@/lib/llm/client` module-load (`new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY! })`) passes in unit tests. Per PATTERNS.md §Shared Pattern 6 rule #6: vitest.setup.ts is the only place env vars are pre-populated at module load.
  </action>
  <verify>
    <automated>test -f scripts/check-hnsw.ts &amp;&amp; grep -q "items_embedding_hnsw_idx" scripts/check-hnsw.ts &amp;&amp; grep -q "check:hnsw" package.json &amp;&amp; grep -q "ANTHROPIC_API_KEY" .env.example &amp;&amp; grep -q "VOYAGE_API_KEY" .env.example &amp;&amp; grep -q "LANGFUSE_PUBLIC_KEY" .env.example &amp;&amp; grep -q "ANTHROPIC_API_KEY" vitest.setup.ts &amp;&amp; grep -q "VOYAGE_API_KEY" vitest.setup.ts &amp;&amp; grep -q "LANGFUSE_PUBLIC_KEY" vitest.setup.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/check-hnsw.ts` exists; grep `items_embedding_hnsw_idx` matches; grep `using hnsw` matches
    - File contains both `process.exit(ok ? 0 : 1)` and the `main().then(...).catch(...)` tail (mirrors verify-ingest.ts:393-400)
    - `package.json` scripts block contains both `"check:hnsw"` AND `"verify:hnsw"` keys, each pointing to `tsx --env-file=.env.local scripts/check-hnsw.ts`
    - `.env.example` contains `ANTHROPIC_API_KEY`, `VOYAGE_API_KEY`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_BASE_URL` (all 5)
    - `vitest.setup.ts` sets all 5 env vars via `??=` (dummy test values); existing DATABASE_URL line untouched
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Script file + package.json scripts + .env.example + vitest.setup.ts all updated; typecheck green. Wave 2 (Plans 02/03) can now import from @/lib/llm/client etc. without module-load failures in tests.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migration 0003 to live Neon dev branch via `pnpm db:push` and verify via `pnpm check:hnsw`</name>
  <what-built>
    Migration file `drizzle/0003_hnsw_index_and_settings_seed.sql` is committed to the repo (Task 1) and the post-push assertion script `scripts/check-hnsw.ts` is registered as `pnpm check:hnsw` (Task 2). The live Neon dev branch does NOT yet have the HNSW index or the cluster_threshold settings row.
  </what-built>
  <why-blocking>
    Without this push, the clustering SQL in Plan 03 (`src/lib/cluster/join-or-create.ts`) using `ORDER BY embedding <=> $1 LIMIT 1` will run as a sequential scan — zero error, just slow — and Plan 05's `pnpm verify:llm` will PASS-by-accident on tiny data, masking the performance contract. The push is MANDATORY per the orchestrator's schema_push_requirement block.

    TypeScript types are generated from `drizzle/schema.ts` (DSL), NOT from the live DB — so `pnpm typecheck` remains green with or without the push. The assertion that the DB actually has the index and the seed row must therefore be done via a live query (`pnpm check:hnsw`), not via `tsc`.

    Additionally, Plan 03's `getClusterThreshold()` (src/lib/cluster/threshold.ts) queries `SELECT value FROM settings WHERE key='cluster_threshold'`. Without the seed row, it falls through to the hard-coded 0.82 default — functionally equivalent at bootstrap but a silent divergence from CLUST-04's "adjustable without redeploy" contract.
  </why-blocking>
  <how-to-verify>
    Run these exact commands in order; all must succeed:

    1. Confirm `.env.local` has a valid `DATABASE_URL` pointing at the Neon DEV branch (Phase 1 D-11 branch-per-PR pattern). Do NOT run against production. Output of this confirmation: `cat .env.local | grep DATABASE_URL | head -1 | sed 's/=.*/=<redacted>/'` — expect one match with the dev-branch URL redacted.

    2. `pnpm drizzle-kit push`
       (equivalent: `pnpm db:push`). On first run against a fresh branch, drizzle-kit MAY prompt for interactive confirmation on the settings INSERT — if non-TTY, fall back to `psql "$DATABASE_URL" -f drizzle/0003_hnsw_index_and_settings_seed.sql` (matches Phase 1 D-11 precedent for raw SQL). For `drizzle-kit push` in non-TTY (CI/automation), the Phase 2 D-21 tsx pattern does not apply here because this is drizzle-kit directly, not a tsx script. If drizzle-kit push complains about a hand-authored file without a journal diff, verify journal entry from Task 1 is correctly appended.

    3. `pnpm check:hnsw`
       Expect stdout: `[PASS] items_embedding_hnsw_idx (HNSW) + settings.cluster_threshold=0.82` and exit code 0.

    4. As a cross-check, also run:
       `psql "$DATABASE_URL" -c "SELECT indexdef FROM pg_indexes WHERE indexname='items_embedding_hnsw_idx';"`
       Expect one row whose `indexdef` contains `USING hnsw (embedding vector_cosine_ops) WITH (m='16', ef_construction='64')`.

    If any step fails, do NOT proceed to Wave 2. Diagnose (check DATABASE_URL, check that `vector` extension is enabled from Phase 1 INFRA-02, check drizzle-kit version), fix, and re-run.

    Type "approved" to unblock Wave 2 (Plans 02 + 03) once all four steps succeed.
  </how-to-verify>
  <files>drizzle/0003_hnsw_index_and_settings_seed.sql (applied to live dev branch — no code change)</files>
  <action>
See <how-to-verify> block above for the exact command sequence. In short: run pnpm drizzle-kit push (or psql fallback) then pnpm check:hnsw. This is a human-gated operational step; no new source code is written.
  </action>
  <verify>
    <automated>pnpm check:hnsw</automated>
  </verify>
  <done>pnpm check:hnsw exits 0 AND human operator has typed "approved".</done>
  <resume-signal>Type "approved" after `pnpm check:hnsw` exits 0, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Neon dev branch | Plan 01 applies DDL via `drizzle-kit push` over the dev-branch DATABASE_URL; boundary is authenticated via Neon connection string secret |
| Migration SQL → production catalog | The 0003 SQL is idempotent (IF NOT EXISTS on the index, ON CONFLICT DO NOTHING on the seed) — re-application cannot corrupt existing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Migration SQL | mitigate | Hand-authored SQL is reviewed in PR; no dynamic values; IF NOT EXISTS + ON CONFLICT DO NOTHING guarantee idempotency — no destructive paths |
| T-03-06 | Info Disclosure | `.env.example` additions | accept | `.env.example` contains only VARIABLE NAMES (no values). The file is tracked in git by design (Phase 1 D-06 canonical registry pattern). No credentials leak. |
| T-03-07 | Info Disclosure | check-hnsw script error paths | mitigate | Script outputs only index metadata (indexname, indexdef) and settings.value — no rows from `items`, no credentials, no environment dumps. `catch` rethrows with `console.error` of the error message only (no process.env dump) |
| T-03-08 | Denial of Service | HNSW index build time | accept | At v1 scale (<10k rows) HNSW build with m=16/ef_construction=64 completes in <1s. `IF NOT EXISTS` prevents rebuild on re-push. No DoS risk. |
| T-03-09 | Tampering | vitest.setup.ts dummy credentials | mitigate | All dummy values use clearly-test prefixes (`sk-ant-test-dummy`, `pa-test-dummy`, `pk-lf-test-dummy`, `sk-lf-test-dummy`) that cannot resolve to real API accounts. `??=` idiom ensures real env vars override at runtime. |
</threat_model>

<verification>
- `pnpm typecheck` green after Task 2 (no schema.ts changes, scripts/check-hnsw.ts typechecks)
- `pnpm check:hnsw` exits 0 against the live Neon dev branch after Task 3
- `grep -q "0003_hnsw_index_and_settings_seed" drizzle/meta/_journal.json` matches
- `diff drizzle/meta/0002_snapshot.json drizzle/meta/0003_snapshot.json` produces zero output (byte-identical)
- Manual: Neon dashboard → dev-branch → Indexes → confirm `items_embedding_hnsw_idx` present
</verification>

<success_criteria>
Plan 01 is DONE when ALL of the following are true:

1. `drizzle/0003_hnsw_index_and_settings_seed.sql` exists and contains HNSW index creation + settings seed.
2. `drizzle/meta/_journal.json` has idx=3 entry with tag `0003_hnsw_index_and_settings_seed`.
3. `drizzle/meta/0003_snapshot.json` exists and is byte-identical to 0002_snapshot.json.
4. `scripts/check-hnsw.ts` exists and registered as both `pnpm check:hnsw` and `pnpm verify:hnsw`.
5. `.env.example` has the 5 Phase 3 env vars documented.
6. `vitest.setup.ts` pre-populates dummy values for the 5 env vars via `??=`.
7. `pnpm typecheck` exits 0.
8. `pnpm check:hnsw` exits 0 on live dev branch (Task 3 human gate).
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-pipeline-clustering/03-01-SUMMARY.md` recording:
- Migration file + journal/snapshot state (include `when` epoch for traceability)
- Any drizzle-kit push output quirks encountered (non-TTY fallback used? interactive prompts?)
- The two dummy env-var naming choices for vitest.setup.ts (documented for downstream plans' tests)
- Commit SHA of the migration commit
</output>
