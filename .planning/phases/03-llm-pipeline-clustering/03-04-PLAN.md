---
phase: 03-llm-pipeline-clustering
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/lib/llm/otel.ts
  - src/trigger/process-pending.ts
  - src/trigger/process-item.ts
  - src/trigger/refresh-clusters.ts
  - src/trigger/index.ts
  - trigger.config.ts
  - src/lib/llm/process-item-core.ts
autonomous: true
requirements: [LLM-01, LLM-11, LLM-13, CLUST-06]
tags: [trigger-dev, otel, langfuse, scheduler, fan-out, debounce]

must_haves:
  truths:
    - "src/trigger/process-pending.ts runs on cron '*/5 * * * *'; atomically claims up to 20 'pending' rows via UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING id; flips claimed rows to status='processing'; fans out via batch.triggerAndWait to 'process-item'"
    - "After fan-out completes AND at least one child succeeded, process-pending enqueues 'refresh-clusters' via trigger with debounce: {key:'refresh-clusters', delay:'60s'} (or the Trigger.dev v4 verified equivalent)"
    - "src/trigger/process-item.ts is a thin adapter: startOtel() at module-load, calls runProcessItem(), flushOtel() in finally{} (LLM-13 — flushes OTel spans to Langfuse before worker recycles)"
    - "src/trigger/process-item.ts has maxDuration=120 and retries configured so transient errors retry up to 3 attempts (LLM-11)"
    - "src/trigger/refresh-clusters.ts is a thin adapter: calls runRefreshClusters(); maxDuration=180"
    - "src/lib/llm/otel.ts bootstraps NodeSDK with LangfuseSpanProcessor + AnthropicInstrumentation; exports startOtel (idempotent) + flushOtel (awaits sdk.shutdown)"
    - "src/trigger/index.ts barrel exports all 3 new tasks alongside existing Phase 1/2 tasks"
    - "process-item-core.ts default joinOrCreateCluster dep is set to the real @/lib/cluster/join-or-create import (wiring step — Wave 2 modules are now available)"
  artifacts:
    - path: "src/lib/llm/otel.ts"
      provides: "Langfuse OTel SDK bootstrap — startOtel / flushOtel"
      exports: ["startOtel", "flushOtel"]
    - path: "src/trigger/process-pending.ts"
      provides: "Scheduled poller — atomic claim + fan-out + refresh-clusters enqueue"
      contains: "schedules.task"
    - path: "src/trigger/process-item.ts"
      provides: "Per-item task — thin adapter over runProcessItem + OTel flush"
      contains: "maxDuration: 120"
    - path: "src/trigger/refresh-clusters.ts"
      provides: "Debounce-invoked bulk bookkeeping task"
      contains: "runRefreshClusters"
    - path: "src/trigger/index.ts"
      provides: "Barrel exports extended with Phase 3 tasks"
      contains: "process-pending"
  key_links:
    - from: "src/trigger/process-pending.ts"
      to: "items table + FOR UPDATE SKIP LOCKED"
      via: "db.execute(sql`UPDATE items SET status='processing' WHERE id IN (...) RETURNING id`)"
      pattern: "FOR UPDATE SKIP LOCKED"
    - from: "src/trigger/process-pending.ts"
      to: "src/trigger/process-item.ts"
      via: "batch.triggerAndWait<typeof processItem>"
      pattern: "batch\\.triggerAndWait"
    - from: "src/trigger/process-item.ts"
      to: "src/lib/llm/process-item-core.ts + src/lib/llm/otel.ts"
      via: "runProcessItem + startOtel/flushOtel"
      pattern: "flushOtel\\(\\)"
    - from: "src/trigger/process-pending.ts"
      to: "src/trigger/refresh-clusters.ts via Trigger.dev debounce"
      via: "refreshClusters.trigger(undefined, { debounce: buildDebounceOpts() }) or verified-equivalent v4 shape"
      pattern: "refresh-clusters|refreshClusters"
---

<objective>
Wire Plans 02 and 03 into live Trigger.dev v4 workers. This plan adds three new tasks under `src/trigger/`, bootstraps Langfuse OTel tracing (LLM-13), and wires process-item-core's cluster dep to Plan 03's real `joinOrCreateCluster`. It's the Wave 3 integration plan — everything before it was library code; this makes the pipeline run.

Scope:
- `src/lib/llm/otel.ts` — Langfuse OTel bootstrap (LLM-13)
- `src/trigger/process-pending.ts` — scheduled poller (LLM-01) with atomic claim + fan-out + debounced refresh-clusters enqueue (CLUST-06)
- `src/trigger/process-item.ts` — thin adapter over runProcessItem with OTel flush (LLM-11 retry semantics)
- `src/trigger/refresh-clusters.ts` — thin adapter over runRefreshClusters
- `src/trigger/index.ts` — barrel exports
- `trigger.config.ts` — (potentially) queue declaration + per-task concurrency
- `src/lib/llm/process-item-core.ts` — wire the real `joinOrCreateCluster` as the default dep (Plan 02 left it as a required inject; now that Plan 03 shipped, we set the real default)

Does NOT include: the live verification harness or UAT (Plan 05).

Open question A8 (Trigger.dev v4 debounce API shape) — planner MUST verify at task time via live SDK docs + node_modules inspection; fallback in RESEARCH.md line 596.

Purpose: Satisfies LLM-01 (Trigger.dev-only pipeline), LLM-11 (retry + dead-letter semantics at task level), LLM-13 (Langfuse traces), CLUST-06 (debounced refresh).
Output: 4 new files + 3 modified files; Trigger.dev CLI can `trigger.dev deploy` this without errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-llm-pipeline-clustering/03-RESEARCH.md
@.planning/phases/03-llm-pipeline-clustering/03-PATTERNS.md
@.planning/phases/03-llm-pipeline-clustering/03-VALIDATION.md
@src/trigger/ingest-hourly.ts
@src/trigger/fetch-source.ts
@src/trigger/health-probe.ts
@src/trigger/index.ts
@trigger.config.ts
@src/lib/llm/process-item-core.ts
@src/lib/cluster/join-or-create.ts
@src/lib/cluster/refresh.ts
@src/lib/ingest/fetch-source-core.ts

<interfaces>
Trigger.dev v4 scheduled task pattern (src/trigger/ingest-hourly.ts — the EXACT template for process-pending):

  import { schedules, batch } from '@trigger.dev/sdk';
  import { eq } from 'drizzle-orm';
  import { db } from '@/lib/db/client';
  import { sources } from '@/lib/db/schema';
  import { fetchSource } from './fetch-source';

  export const ingestHourly = schedules.task({
    id: 'ingest-hourly',
    cron: '0 * * * *',
    run: async (payload) => { /* enumerate → batch.triggerAndWait → aggregate */ },
  });

Trigger.dev v4 task pattern (src/trigger/fetch-source.ts):

  import { task } from '@trigger.dev/sdk';
  import { runFetchSource, type FetchSourceResult } from '@/lib/ingest/fetch-source-core';

  export const fetchSource = task({
    id: 'fetch-source',
    maxDuration: 90,
    run: async (payload: { sourceId: number; rssUrl: string }): Promise<FetchSourceResult> => {
      return runFetchSource({ sourceId: payload.sourceId, rssUrl: payload.rssUrl });
    },
  });

batch.triggerAndWait v4 signature (STATE.md — verified @trigger.dev/sdk@4.4.4 shared.d.ts:232):
  Array<{ id: string; payload: P }> — id is the STRING task id, not the object.
  Returns { runs: Array<{ ok, output } | { ok:false, error }> }.

atomic claim SQL (RESEARCH.md §Pattern 3 + Pitfall 7):
  UPDATE items SET status='processing'
  WHERE id IN (
    SELECT id FROM items WHERE status='pending'
    ORDER BY ingested_at ASC LIMIT $batchSize
    FOR UPDATE SKIP LOCKED
  )
  RETURNING id

Langfuse OTel bootstrap (RESEARCH.md §Langfuse OTel bootstrap lines 428-472):
  import { NodeSDK } from '@opentelemetry/sdk-node';
  import { LangfuseSpanProcessor } from '@langfuse/otel';
  import { AnthropicInstrumentation } from '@arizeai/openinference-instrumentation-anthropic';
  import Anthropic from '@anthropic-ai/sdk';

  const instrumentation = new AnthropicInstrumentation();
  instrumentation.manuallyInstrument(Anthropic);

  const otel = new NodeSDK({ spanProcessors: [new LangfuseSpanProcessor()], instrumentations: [instrumentation] });

  let started = false;
  export function startOtel() { if (started) return; otel.start(); started = true; }
  export async function flushOtel() { await otel.shutdown(); }

Trigger.dev v4 debounce (RESEARCH.md Assumption A8 — VERIFY AT TASK TIME):
  Expected shape: refreshClusters.trigger(undefined, { debounce: { key: 'refresh-clusters', delay: '60s' } })
  Fallback (if v4 shape differs): write a settings row `last_refresh_cluster_enqueue_at` + coalesce in-task (read this row at refresh start; no-op if within 60s of previous run).

trigger.config.ts current shape (existing):
  export default defineConfig({
    project: process.env.TRIGGER_PROJECT_REF!,
    dirs: ['./src/trigger'],
    runtime: 'node',
    maxDuration: 3600,
    retries: { enabledInDev: false, default: { maxAttempts: 3, ... } },
  });

If queue-level concurrency is a v4 feature on task() options (RESEARCH.md §Architecture: queue 'llm-pipeline', concurrencyLimit 4), declare on the process-item task directly. If it lives in trigger.config.ts, add a queues: [...] block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install OTel deps + create src/lib/llm/otel.ts + wire process-item-core default joinOrCreateCluster</name>
  <files>package.json, src/lib/llm/otel.ts, src/lib/llm/process-item-core.ts</files>
  <read_first>
    - 03-RESEARCH.md §Langfuse OTel bootstrap (lines 428-472) + §Pitfall 6 (OTel flush)
    - 03-PATTERNS.md §src/lib/llm/otel.ts (no-analog reference — spec-driven)
    - src/lib/rsshub.ts (env-var access pattern; LANGFUSE_* env must be read at module load but guarded — startOtel() is idempotent so bootstrap-time calls are safe)
    - src/lib/llm/process-item-core.ts (Plan 02 output — currently has `const joinFn = params.deps?.joinOrCreateCluster` with no default; this task wires the default)
    - src/lib/cluster/join-or-create.ts (Plan 03 output — provides the real function)
  </read_first>
  <action>
(a) Install OTel deps:
    pnpm add @langfuse/otel @arizeai/openinference-instrumentation-anthropic @opentelemetry/sdk-node

Verify via `pnpm why @langfuse/otel` that no peerDep conflicts exist (@opentelemetry/api is a common peer — if it warns, `pnpm add @opentelemetry/api` to explicitly resolve).

(b) Create `src/lib/llm/otel.ts` per RESEARCH.md spec:

    /**
     * Langfuse OTel bootstrap — Phase 3 LLM-13.
     *
     * Wires Anthropic SDK → OpenInference instrumentation → Langfuse span
     * processor → OTel NodeSDK. Captures request/response/usage (including
     * cache_read/cache_write tokens) on every Anthropic call automatically.
     *
     * Pitfall 6 (RESEARCH.md): Trigger.dev recycles workers aggressively;
     * OTel spans sit in batch queue until flushed. `flushOtel()` MUST be
     * called in the task's finally{} block before worker recycle.
     *
     * startOtel() is idempotent (guarded by a module-level `started` flag) —
     * safe to call at module load in every trigger task file.
     *
     * Consumed by:
     *   - src/trigger/process-item.ts (Plan 04)
     *   - src/trigger/process-pending.ts (if wanting scheduler-level trace)
     *   - src/trigger/refresh-clusters.ts (if wanting debounce-task trace)
     */
    import { NodeSDK } from '@opentelemetry/sdk-node';
    import { LangfuseSpanProcessor } from '@langfuse/otel';
    import { AnthropicInstrumentation } from '@arizeai/openinference-instrumentation-anthropic';
    import Anthropic from '@anthropic-ai/sdk';

    const instrumentation = new AnthropicInstrumentation();
    instrumentation.manuallyInstrument(Anthropic);

    const otel = new NodeSDK({
      spanProcessors: [new LangfuseSpanProcessor()],
      instrumentations: [instrumentation],
    });

    let started = false;
    export function startOtel(): void {
      if (started) return;
      otel.start();
      started = true;
    }

    export async function flushOtel(): Promise<void> {
      await otel.shutdown();
    }

If any of the three packages' API differs from RESEARCH.md (names, constructor signatures), read the actual .d.ts and adapt. The non-negotiable behaviors:
  - Call AnthropicInstrumentation().manuallyInstrument(Anthropic) BEFORE any Anthropic client instantiation (startOtel must be called before `new Anthropic(...)` — in practice that means before `src/lib/llm/client.ts` module-loads in the same process. Document any ordering requirement in plan summary.)
  - NodeSDK configured with Langfuse span processor as one of the span processors.
  - shutdown() flushes pending spans.

(c) Edit `src/lib/llm/process-item-core.ts` (Plan 02 file) — provide the REAL default for the `joinOrCreateCluster` dep:

Change the ProcessItemDeps interface from having an OPTIONAL injection to having a DEFAULT import:

Before (Plan 02):
    import { enrichWithClaude as realEnrich, EnrichError } from './enrich';
    import { embedDocument as realEmbed, EmbedError } from './embed';
    // ...
    const joinFn = params.deps?.joinOrCreateCluster; // Plan 03 provides at prod; tests MUST inject.
    if (!joinFn) { throw new Error('joinOrCreateCluster dep not provided'); }

After (Plan 04):
    import { joinOrCreateCluster as realJoinOrCreateCluster } from '@/lib/cluster/join-or-create';
    // ... in runProcessItem:
    const joinFn = params.deps?.joinOrCreateCluster ?? (async (p) => realJoinOrCreateCluster(p));

Also remove the `throw new Error('joinOrCreateCluster dep not provided')` guard since the default now resolves. Update process-item-core.test.ts if needed (tests should still inject mocks — the `deps?.joinOrCreateCluster ?? real` fallback pattern is test-compatible).

Run `pnpm test --run src/lib/llm src/lib/cluster` to ensure no regression.
  </action>
  <verify>
    <automated>test -f src/lib/llm/otel.ts &amp;&amp; grep -q "@langfuse/otel" package.json &amp;&amp; grep -q "@arizeai/openinference-instrumentation-anthropic" package.json &amp;&amp; grep -q "@opentelemetry/sdk-node" package.json &amp;&amp; grep -q "startOtel" src/lib/llm/otel.ts &amp;&amp; grep -q "flushOtel" src/lib/llm/otel.ts &amp;&amp; grep -q "manuallyInstrument" src/lib/llm/otel.ts &amp;&amp; grep -q "joinOrCreateCluster as realJoinOrCreateCluster" src/lib/llm/process-item-core.ts &amp;&amp; pnpm test --run src/lib/llm src/lib/cluster &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/llm/otel.ts` exists; exports `startOtel` + `flushOtel`
    - Grep: `manuallyInstrument(Anthropic)` present in otel.ts
    - Grep: `let started = false` (idempotence guard)
    - Grep: `await otel.shutdown()` (flush semantic)
    - package.json contains all 3 OTel deps
    - `src/lib/llm/process-item-core.ts` imports `joinOrCreateCluster` from `@/lib/cluster/join-or-create`
    - `src/lib/llm/process-item-core.ts` no longer throws `'joinOrCreateCluster dep not provided'` — it uses real as default
    - All Plan 02 + Plan 03 tests still pass
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>OTel module ships; process-item-core wired to real cluster impl; Plan 02/03 tests still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: src/trigger/process-item.ts + src/trigger/refresh-clusters.ts + trigger.config.ts queue</name>
  <files>src/trigger/process-item.ts, src/trigger/refresh-clusters.ts, trigger.config.ts</files>
  <read_first>
    - src/trigger/fetch-source.ts (thin-wrapper pattern — EXACT template; lines 1-30)
    - src/trigger/health-probe.ts (first task-pattern example)
    - trigger.config.ts (existing config — project-level maxDuration=3600; per-task override precedent documented)
    - 03-RESEARCH.md §Langfuse OTel bootstrap usage in process-item.ts (lines 457-472)
    - 03-PATTERNS.md §src/trigger/process-item.ts + §src/trigger/refresh-clusters.ts
    - node_modules/@trigger.dev/sdk/dist/**/tasks.d.ts — read to VERIFY whether `queue` option lives on task({...}) or in defineConfig({queues:[...]})
  </read_first>
  <action>
(a) Verify via node_modules inspection: does `task({ id, maxDuration, queue, run })` accept a `queue` option in @trigger.dev/sdk@4.4.4? Search for the type def:
    grep -rn "concurrencyLimit\|queue" node_modules/@trigger.dev/sdk/dist/commonjs/v3/*.d.ts | head -20
    grep -rn "queues" node_modules/@trigger.dev/core/dist/commonjs/v3/*.d.ts | head -20

Based on findings, decide:
  - Option A: queue option on task() — add `queue: { name: 'llm-pipeline', concurrencyLimit: 4 }` to process-item task
  - Option B: queue declared in defineConfig — add `queues: [{ name: 'llm-pipeline', concurrencyLimit: 4 }]` to trigger.config.ts, then reference by name via `queue: 'llm-pipeline'` in task
  - Option C: feature not available as declarative config in 4.4.4 — fall back to `trigger.config.ts` retries block acting as the global concurrency shape, document concurrency as Trigger.dev Cloud dashboard setting

Document the choice in plan summary.

(b) Create `src/trigger/process-item.ts` — mirror `src/trigger/fetch-source.ts:1-30` + PATTERNS.md §src/trigger/process-item.ts shape:

    import { task } from '@trigger.dev/sdk';
    import { runProcessItem, type ProcessItemResult } from '@/lib/llm/process-item-core';
    import { startOtel, flushOtel } from '@/lib/llm/otel';

    // OTel bootstrap at module load — idempotent (RESEARCH.md §Pitfall 6).
    startOtel();

    /**
     * Per-item LLM-pipeline worker — Phase 3 LLM-01, LLM-11, LLM-13.
     *
     * Thin adapter over src/lib/llm/process-item-core.ts. Responsibilities:
     *   - Ensure OTel is running (startOtel at module load).
     *   - Invoke runProcessItem with the payload itemId.
     *   - In finally{}, call flushOtel() so Langfuse spans reach the dashboard
     *     before the Trigger.dev runtime recycles the worker process.
     *
     * Retries: maxAttempts=3 (inherited from trigger.config.ts retries block).
     * LLM-11 terminal conditions (ZodError, EnrichError 'parse'/'schema',
     * EmbedError malformed) are handled INSIDE runProcessItem — the core sets
     * status='dead_letter' and RETURNS (no throw). Non-terminal thrown errors
     * re-throw here and Trigger.dev retries. When retries are exhausted,
     * process-item-core (on the next invocation) sees retryCount >= 3 and
     * flips status='dead_letter' itself.
     *
     * maxDuration=120s: budget is ~15s extract + ~30s Haiku + ~3s Voyage +
     * ~5s cluster transaction + padding.
     *
     * Consumed by:
     *   - src/trigger/process-pending.ts (batch.triggerAndWait fan-out)
     *   - Trigger.dev dashboard manual-run for debugging a single item
     */
    export const processItem = task({
      id: 'process-item',
      maxDuration: 120,
      // Queue declaration depends on verification above — add here if Option A.
      run: async (payload: { itemId: string }): Promise<ProcessItemResult> => {
        try {
          return await runProcessItem({ itemId: BigInt(payload.itemId) });
        } finally {
          await flushOtel();
        }
      },
    });

(c) Create `src/trigger/refresh-clusters.ts` — mirror fetch-source.ts shape:

    import { task } from '@trigger.dev/sdk';
    import { runRefreshClusters, type RefreshClustersResult } from '@/lib/cluster/refresh';

    /**
     * Debounced cluster bookkeeping — Phase 3 CLUST-05, CLUST-06, CLUST-07.
     *
     * Enqueued by src/trigger/process-pending.ts with a debounce of 60s and
     * key 'refresh-clusters' — coalesces a burst of new items into one refresh
     * run 60s after the last trigger (trailing-edge).
     *
     * maxDuration=180s: budget for iterating every dirty cluster (<= 200 at
     * v1 scale) + AVG(embedding) per cluster. Far above expected p95.
     *
     * Consumed by:
     *   - src/trigger/process-pending.ts (post-fan-out, with debounce)
     *   - Trigger.dev dashboard manual-run for debugging
     */
    export const refreshClusters = task({
      id: 'refresh-clusters',
      maxDuration: 180,
      run: async (): Promise<RefreshClustersResult> => runRefreshClusters(),
    });

(d) Edit `trigger.config.ts` — if Option B from Task 2(a) was chosen, add `queues: [{ name: 'llm-pipeline', concurrencyLimit: 4 }]` to the defineConfig object. Preserve existing `retries`, `maxDuration`, `dirs`, `project`, `runtime`. If Option A/C, leave trigger.config.ts unchanged and document in plan summary that concurrency is declared on the task itself (Option A) or in Trigger.dev Cloud dashboard (Option C).

  </action>
  <verify>
    <automated>test -f src/trigger/process-item.ts &amp;&amp; test -f src/trigger/refresh-clusters.ts &amp;&amp; grep -q "startOtel" src/trigger/process-item.ts &amp;&amp; grep -q "flushOtel" src/trigger/process-item.ts &amp;&amp; grep -q "maxDuration: 120" src/trigger/process-item.ts &amp;&amp; grep -q "maxDuration: 180" src/trigger/refresh-clusters.ts &amp;&amp; grep -q "runRefreshClusters" src/trigger/refresh-clusters.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/trigger/process-item.ts` exists; exports `processItem`
    - Grep `startOtel()` present at module scope (module-load bootstrap)
    - Grep `finally { await flushOtel()` or `finally {` followed by `flushOtel` on next line (OTel flush contract — LLM-13 Pitfall 6)
    - Grep `maxDuration: 120` present
    - `src/trigger/refresh-clusters.ts` exists; exports `refreshClusters`; grep `maxDuration: 180`
    - `src/trigger/refresh-clusters.ts` calls `runRefreshClusters()` (CLUST-05/07 impl from Plan 03)
    - Plan summary documents the queue/concurrency decision (Option A/B/C)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>process-item + refresh-clusters tasks ship; OTel bootstrap + flush wired; queue decision documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: src/trigger/process-pending.ts (atomic claim + fan-out + debounced refresh) + src/trigger/index.ts barrel</name>
  <files>src/trigger/process-pending.ts, src/trigger/index.ts</files>
  <read_first>
    - src/trigger/ingest-hourly.ts (EVERY LINE — exact template, swap `sources` enumeration for atomic-claim, swap `fetchSource` for `processItem`; lines 1-91)
    - src/trigger/index.ts (Phase 2 additions section — extend conventions; lines 1-7)
    - src/lib/db/schema.ts:36-74 (items columns — id is bigserial; use sql.execute because Drizzle doesn't expose UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING directly)
    - 03-RESEARCH.md §Pattern 3 (atomic poller — lines 315-338) + §Pitfall 7 (SKIP LOCKED correctness)
    - 03-RESEARCH.md §Cluster-refresh flow (lines 197-201) — debounce call shape
    - 03-RESEARCH.md §Open Questions #5 (batchSize=20) + §Overview cron '*/5'
    - 03-PATTERNS.md §src/trigger/process-pending.ts
    - src/lib/cluster/refresh.ts (from Plan 03 — import buildDebounceOpts helper)
    - node_modules/@trigger.dev/sdk/dist/commonjs/v3/**/*.d.ts — VERIFY debounce API shape per Assumption A8 (check task.trigger() options type for `debounce?: { key: string; delay: string }`)
  </read_first>
  <action>
(a) VERIFY A8: read the Trigger.dev v4 SDK .d.ts files to confirm the trigger/triggerAndWait options include debounce. Grep for `debounce` in node_modules/@trigger.dev/sdk/dist and @trigger.dev/core. Document findings in plan summary. If debounce option is absent at 4.4.4, use the fallback: insert/update a `settings` row `last_cluster_refresh_enqueue_at` + skip enqueue when within 60s of previous value.

(b) Create `src/trigger/process-pending.ts` — mirror `src/trigger/ingest-hourly.ts` with substitutions:

    import { schedules, batch } from '@trigger.dev/sdk';
    import { sql } from 'drizzle-orm';
    import { db } from '@/lib/db/client';
    import { processItem } from './process-item';
    import { refreshClusters } from './refresh-clusters';
    import { buildDebounceOpts } from '@/lib/cluster/refresh';

    const BATCH_SIZE = 20; // RESEARCH.md Open Q #5

    /**
     * LLM-pipeline scheduled poller — Phase 3 LLM-01, CLUST-06.
     *
     * Cron: `*​/5 * * * *` (every 5 minutes, UTC). (Slash deliberately preserved.)
     *
     * Topology:
     *   1. Atomic claim: UPDATE items SET status='processing' WHERE id IN (
     *        SELECT id FROM items WHERE status='pending' ORDER BY ingested_at
     *        LIMIT 20 FOR UPDATE SKIP LOCKED
     *      ) RETURNING id.
     *      FOR UPDATE SKIP LOCKED (RESEARCH.md Pitfall 7) prevents double
     *      pickup if two pollers ever overlap.
     *   2. Fan out via batch.triggerAndWait → one process-item run per claimed id.
     *      Queue 'llm-pipeline' + concurrency 4 (declared on process-item task
     *      or in trigger.config.ts per Plan 04 Task 2 Option A/B).
     *   3. If >=1 child succeeded, enqueue refresh-clusters with debounce
     *      (key='refresh-clusters', delay='60s') so bursts coalesce into one
     *      bookkeeping run 60s after last enqueue (CLUST-06).
     *
     * LLM-11: retry budget lives on the process-item task (maxAttempts=3).
     * Terminal errors (ZodError, malformed LLM response) are handled inside
     * runProcessItem — children return { status: 'dead_letter' } rather than
     * throw, so batch.triggerAndWait sees them as `ok: true` with a dead-letter
     * output. Successes are tallied the same way.
     *
     * LLM-13: OTel startOtel is called at each process-item's module load;
     * flushOtel in its finally. This scheduler does not need its own OTel wiring.
     *
     * Consumed by:
     *   - Trigger.dev scheduler (automatic, every 5 min)
     *   - Trigger.dev dashboard manual-trigger for catch-up runs
     */
    export const processPending = schedules.task({
      id: 'process-pending',
      cron: '*/5 * * * *',
      run: async (payload) => {
        // STEP 1 — atomic claim via raw SQL (drizzle-orm does not expose
        // UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING,
        // so we go through db.execute).
        const claimed = (await db.execute(sql`
          UPDATE items SET status = 'processing'
          WHERE id IN (
            SELECT id FROM items
            WHERE status = 'pending'
            ORDER BY ingested_at ASC
            LIMIT ${BATCH_SIZE}
            FOR UPDATE SKIP LOCKED
          )
          RETURNING id
        `)) as unknown as { rows: Array<{ id: string }> };
        const ids = claimed.rows.map((r) => r.id);

        if (ids.length === 0) {
          return { scheduledAt: payload.timestamp, claimed: 0, successes: 0, deadLetters: 0, failures: 0 };
        }

        // STEP 2 — fan out
        const batchItems = ids.map((id) => ({
          id: 'process-item' as const,
          payload: { itemId: id },
        }));
        const result = await batch.triggerAndWait<typeof processItem>(batchItems);

        let successes = 0;
        let deadLetters = 0;
        let failures = 0;
        let anyPublished = false;
        for (const run of result.runs) {
          if (!run.ok) {
            failures += 1;
            continue;
          }
          if (run.output?.status === 'published') {
            successes += 1;
            anyPublished = true;
          } else if (run.output?.status === 'dead_letter') {
            deadLetters += 1;
          }
        }

        // STEP 3 — debounced refresh-clusters enqueue (only when at least one
        // item actually published; dead-letters and failures don't mutate clusters).
        if (anyPublished) {
          const opts = buildDebounceOpts();
          try {
            // Trigger.dev v4 debounce — verified shape at plan time (A8).
            // If shape differs, planner substituted the verified form here.
            await refreshClusters.trigger(undefined, { debounce: opts });
          } catch (err) {
            // Debounce enqueue failure is non-fatal — next scheduler tick will retry.
            console.error(
              `refresh-clusters debounce enqueue failed: ${err instanceof Error ? err.name : 'unknown'}`,
            );
          }
        }

        return {
          scheduledAt: payload.timestamp,
          claimed: ids.length,
          successes,
          deadLetters,
          failures,
        };
      },
    });

Fallback if A8 verification shows v4 does not accept `{ debounce: { key, delay } }` on `.trigger(...)`:

  Replace the STEP 3 block with:

    if (anyPublished) {
      // In-task coalesce fallback (RESEARCH.md Assumption A8 fallback).
      // Write a settings row; refreshClusters itself reads it and no-ops if
      // called within 60s of last run.
      const nowIso = new Date().toISOString();
      await db.execute(sql`
        INSERT INTO settings (key, value) VALUES ('last_refresh_cluster_enqueue_at', ${nowIso})
        ON CONFLICT (key) DO UPDATE SET value = ${nowIso}, updated_at = now()
      `);
      await refreshClusters.trigger(undefined);
    }

Document which path was taken in plan summary.

(c) Edit `src/trigger/index.ts` — extend the existing "Phase N additions" convention. Current file:

    // Barrel export — add new task exports here as phases grow.
    // Kept as a single surface so API routes and type-only importers have one import path.
    export * from './health-probe';
    // Phase 2 additions (ingestion pipeline):
    export * from './ingest-hourly';
    export * from './fetch-source';

Append:

    // Phase 3 additions (LLM pipeline + clustering):
    export * from './process-pending';
    export * from './process-item';
    export * from './refresh-clusters';
  </action>
  <verify>
    <automated>test -f src/trigger/process-pending.ts &amp;&amp; grep -q "FOR UPDATE SKIP LOCKED" src/trigger/process-pending.ts &amp;&amp; grep -q "batch.triggerAndWait" src/trigger/process-pending.ts &amp;&amp; grep -q "*/5 * * * *" src/trigger/process-pending.ts &amp;&amp; grep -q "BATCH_SIZE = 20" src/trigger/process-pending.ts &amp;&amp; grep -q "process-pending" src/trigger/index.ts &amp;&amp; grep -q "process-item" src/trigger/index.ts &amp;&amp; grep -q "refresh-clusters" src/trigger/index.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm test --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/trigger/process-pending.ts` exists; exports `processPending`
    - Grep: `FOR UPDATE SKIP LOCKED` present (Pitfall 7 correctness)
    - Grep: `*/5 * * * *` present (cron)
    - Grep: `batch.triggerAndWait` present (fan-out — LLM-01 contract)
    - Grep: `BATCH_SIZE = 20` (Open Q #5 default)
    - Grep: the file references either `{ debounce: opts }` (path A) OR `last_refresh_cluster_enqueue_at` (path B — documented fallback)
    - `src/trigger/index.ts` contains `process-pending`, `process-item`, `refresh-clusters` export lines
    - `pnpm typecheck` exits 0
    - `pnpm test --run` (full suite) exits 0 — no test regressions
    - Plan summary documents A8 resolution (debounce on .trigger() works? OR fallback used?)
  </acceptance_criteria>
  <done>process-pending + barrel updated; FOR UPDATE SKIP LOCKED correct; debounce path chosen and documented; full suite green; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → DB atomic claim | `FOR UPDATE SKIP LOCKED` serializes row pickup across parallel pollers |
| Process-item worker → Langfuse | OTel spans include prompt + completion — sensitive if prompts leak user PII (not v1 — only RSS feeds, all public content) |
| Trigger.dev Cloud → env secrets | LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, ANTHROPIC_API_KEY, VOYAGE_API_KEY all live in Trigger.dev Cloud env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Info Disclosure (Langfuse leak) | `src/lib/llm/otel.ts` | mitigate | OpenInference Anthropic instrumentation captures request/response ONLY — API keys live in SDK client headers, not in the message content that spans capture. Plan summary documents dev-test: make 1 Haiku call, open trace in Langfuse, inspect input/output, confirm no `sk-ant-` prefix appears. Langfuse project is private (auth-gated dashboard). |
| T-03-18 | Concurrency (double-pickup race) | `src/trigger/process-pending.ts` | mitigate | `FOR UPDATE SKIP LOCKED` in the inner SELECT guarantees at most one worker claims a given `pending` row. Only one `process-pending` scheduled task (cron */5) — no duplicate schedule. |
| T-03-19 | Info Disclosure (scheduler error logs) | `process-pending.ts` catch block for debounce enqueue | mitigate | `console.error` logs only `err.name` (never err.message) — consistent with src/lib/rsshub.ts:68-71 scrub pattern. |
| T-03-20 | Denial of Service (runaway fan-out) | `batch.triggerAndWait` | mitigate | `BATCH_SIZE = 20` cap per tick; 5-min cron = 240 items/hour ceiling. At nominal ingestion rate (~50-200/hour) there's 20%+ buffer. Cluster queue concurrency limit (4) prevents Haiku rate-limit burst. |
| T-03-21 | Resource abuse (OTel span queue bloat on worker) | `process-item.ts` | mitigate | `flushOtel()` in `finally{}` after each run bounds in-memory span queue to one run's worth. Pitfall 6 mitigated structurally. |
| T-03-22 | Logic error (refresh-clusters never fires) | `processPending` debounce enqueue | mitigate | STEP 3 only fires when `anyPublished`; if the verified debounce API differs, fallback path uses settings row + immediate trigger. At worst-case (debounce fails silently) the next scheduler tick re-enqueues. Admin-observable via Trigger.dev dashboard. |

No `high` severity open.
</threat_model>

<verification>
- `pnpm typecheck` green
- `pnpm test --run` green (no regression in library tests from Plans 02/03)
- `pnpm lint` green (trigger files follow existing conventions)
- Manual: `pnpm dlx trigger.dev@latest dev` successfully registers all 3 new tasks (developer verifies task list in Trigger.dev local dev UI)
- All grep assertions pass
</verification>

<success_criteria>
Plan 04 DONE when:

1. `src/lib/llm/otel.ts` + 3 trigger tasks + barrel extended + process-item-core wired to Plan 03
2. A8 (debounce) verified and resolved (either v4 supports or fallback in place)
3. Queue/concurrency decision documented (Option A/B/C from Task 2)
4. `pnpm typecheck` + `pnpm test --run` exit 0
5. All 4 must_haves REQ-IDs covered (LLM-01, LLM-11, LLM-13, CLUST-06)
6. `trigger.dev deploy` (dry-run via `pnpm trigger:dev`) lists the 3 new tasks without registration errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-pipeline-clustering/03-04-SUMMARY.md` recording:
- A8 verification outcome (debounce path A or fallback path B taken?)
- Queue/concurrency decision and exact location (task option vs trigger.config.ts vs dashboard)
- OTel dep versions installed (major/minor)
- Any OTel bootstrap ordering issues encountered (startOtel must precede Anthropic client instantiation?)
</output>
