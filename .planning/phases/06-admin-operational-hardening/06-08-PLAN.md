---
phase: 06-admin-operational-hardening
plan: "08"
plan_number: 8
type: execute
wave: 3
depends_on: ["06-00", "06-01", "06-02", "06-03", "06-04", "06-05", "06-06", "06-07"]
files_modified:
  - docs/admin.md
  - docs/observability.md
  - scripts/verify-admin-ops.ts
  - package.json
  - .planning/phases/06-admin-operational-hardening/06-UAT.md
autonomous: false
requirements: [OPS-02]
must_haves:
  truths:
    - "Langfuse dashboard shows per-item LLM cost AND cache hit rate for recent pipeline runs (OPS-02 — no code change; Phase 3 already instrumented Langfuse OTel traces)"
    - "docs/observability.md documents how to read Langfuse dashboards: per-item trace view, cost column, cache hit rate column, cost-per-model breakdown"
    - "docs/admin.md documents: how to promote a user to admin via SQL, how to access /admin, how to ban a user, how to retry a dead-letter item, how to read /admin/costs"
    - "scripts/verify-admin-ops.ts asserts all 5 Phase 6 ROADMAP success criteria programmatically (similar to verify-llm.ts harness)"
    - "06-UAT.md contains a human checklist proving all 5 SCs end-to-end against a live deployment"
  artifacts:
    - path: "docs/observability.md"
      provides: "Langfuse + Sentry operational runbook"
    - path: "docs/admin.md"
      provides: "Admin operations runbook (Chinese + English sections as needed)"
    - path: "scripts/verify-admin-ops.ts"
      provides: "Automated SC harness for Phase 6"
    - path: ".planning/phases/06-admin-operational-hardening/06-UAT.md"
      provides: "Human UAT checklist for Phase 6"
  key_links:
    - from: "scripts/verify-admin-ops.ts"
      to: "Phase 6 ROADMAP SC #1-#5"
      via: "SC-shaped assertion harness"
      pattern: "SC#"
    - from: "06-UAT.md"
      to: "docs/admin.md + docs/observability.md"
      via: "procedural references"
      pattern: "docs/admin|docs/observability"
---

<objective>
Close Phase 6 by documenting the operational surfaces (admin runbook + observability runbook), confirming Langfuse dashboards satisfy OPS-02 (no code change needed — Phase 3 already wired OTel + Langfuse), and shipping a verification harness + UAT checklist that proves all 5 Phase 6 ROADMAP success criteria end-to-end.

Purpose: Phase 6 is the operational phase — its value isn't just code, it's the ability for the user (acting as admin) to operate the system confidently. Without runbooks, the /admin/costs page is data with no interpretation; without a UAT, nothing proves the full loop works on a real deployment.
Output: 2 runbooks + 1 verification script + 1 UAT checklist + OPS-02 documented (no code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/06-admin-operational-hardening/06-00-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-01-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-02-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-03-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-04-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-05-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-06-PLAN.md
@.planning/phases/06-admin-operational-hardening/06-07-PLAN.md
@.planning/phases/03-llm-pipeline-clustering/03-04-SUMMARY.md
@.planning/phases/03-llm-pipeline-clustering/03-UAT.md
@scripts/verify-llm.ts
@docs/auth-providers.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/admin.md + docs/observability.md runbooks</name>
  <files>docs/admin.md, docs/observability.md</files>
  <read_first>docs/auth-providers.md, docs/database.md, README.md, src/lib/llm/otel.ts, .env.example, .planning/phases/06-admin-operational-hardening/06-03-PLAN.md, .planning/phases/06-admin-operational-hardening/06-04-PLAN.md, .planning/phases/06-admin-operational-hardening/06-05-PLAN.md, .planning/phases/06-admin-operational-hardening/06-06-PLAN.md</read_first>
  <action>
Create `docs/admin.md` (English prose, Chinese for quoted UI copy — matches Phase 5 runbook convention):

Sections:
1. Overview — what /admin does, who should have it
2. Promoting a user to admin — SQL snippet: `UPDATE users SET role='admin' WHERE email='admin@example.com';` with cautions (must be done out-of-band; no API surface in v1)
3. Source management (/admin/sources) — add RSSHub route, weight semantics, category values, soft-delete vs toggle-active distinction, health badge interpretation (red = consecutive_empty_count>=3 OR consecutive_error_count>=3)
4. User management (/admin/users) — ban flow (what it does: deletes sessions, sets audit trail; what it does NOT do: delete data); unban flow; admin-to-admin ban limitation
5. Dead-letter queue (/admin/dead-letter) — when items end up there, how retry works, rate limit (20/60s per admin)
6. LLM cost dashboard (/admin/costs) — how to read the rollup, cache-hit ratio interpretation, red-flag thresholds
7. Troubleshooting — admin can't see /admin (role mismatch), retry rate-limited, session stuck banned

Create `docs/observability.md`:

Sections:
1. Sentry — DSN env vars, how to find errors, how PII scrub works, how to verify new errors land within 5 min
2. Langfuse (OPS-02) — how the Phase 3 OTel pipeline feeds Langfuse, how to read per-item traces, cost column, cache hit rate, cost-per-model view; include direct URL template `https://cloud.langfuse.com/project/<PROJECT_ID>/traces`; reference that no code change was needed for OPS-02 because Phase 3 plan 03-04 shipped the OTel bootstrap
3. Vercel Analytics (OPS-05) — dashboard location, metrics surfaced, no-cookie-banner note
4. /api/health — existing Phase 1 endpoint; what it checks (DB, Redis, Trigger.dev, RSSHub)
5. pipeline_runs table as source of truth for cost (same view as /admin/costs) — SQL snippet to query directly: `SELECT date_trunc('day', created_at), model, SUM(estimated_cost_usd) FROM pipeline_runs WHERE status='ok' GROUP BY 1, 2 ORDER BY 1 DESC LIMIT 30;`

Both files use absolute file paths and include "Status: current as of Phase 6 completion" banner.

Append "Further Reading" links to README.md at the end (check current README for the Phase 1+5 doc list and extend).
  </action>
  <verify>
    <automated>test -f docs/admin.md && test -f docs/observability.md && grep -q "Langfuse" docs/observability.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/admin.md exists
    - `grep -q "UPDATE users SET role='admin'" docs/admin.md` returns 0
    - `grep -q "consecutive_empty_count" docs/admin.md` returns 0 (health badge doc)
    - `grep -q "20/60" docs/admin.md` OR `grep -q "20 retries" docs/admin.md` returns 0 (rate limit doc)
    - docs/observability.md exists
    - `grep -q "Langfuse" docs/observability.md` returns 0
    - `grep -q "beforeSend" docs/observability.md` returns 0
    - `grep -q "cloud.langfuse.com" docs/observability.md` returns 0
    - README.md references both new docs under Further Reading (if README has that section)
  </acceptance_criteria>
  <done>Both runbooks exist, cover every /admin route + all 3 observability tools, reference Phase 3 Langfuse wiring.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/verify-admin-ops.ts + 06-UAT.md</name>
  <files>scripts/verify-admin-ops.ts, package.json, .planning/phases/06-admin-operational-hardening/06-UAT.md</files>
  <read_first>scripts/verify-llm.ts, scripts/verify-ingest.ts, .planning/phases/03-llm-pipeline-clustering/03-UAT.md, .planning/phases/06-admin-operational-hardening/06-00-PLAN.md, .planning/phases/06-admin-operational-hardening/06-01-PLAN.md</read_first>
  <action>
Create `scripts/verify-admin-ops.ts` modeled on `scripts/verify-llm.ts` (record(name, pass, detail) pattern + try/finally cleanup + main().then tail). Assertions:

- **SC#1 — admin route gating (WARNING-10 fix: unit-level, NOT HTTP probe):**
  - Dynamically import `requireAdmin` from `@/lib/auth/admin` and mock `auth()` from `@/lib/auth` via vitest-style spy (or Node `--experimental-vm-modules` dynamic module mock) to return: (a) null session, (b) a non-admin session, (c) an admin session.
  - Mock `redirect` from `next/navigation` to throw a sentinel `RedirectError('path')` so the harness can assert the destination.
  - Assert: null session → redirect('/'); non-admin → redirect('/admin/access-denied'); admin → session returned without redirect.
  - This mirrors the 06-00 Task 1 unit test pattern — no dev server dependency, no ECONNREFUSED handling needed.

- **SC#2 — source CRUD + health:**
  - Create a test source via createSourceCore with consecutive_error_count=3; call computeSourceHealth; expect 'red'.
  - softDeleteSourceCore on that source; call listSourcesForAdmin; expect the source does NOT appear.
  - Verify the ingestion poller query would skip it (assert sources.deletedAt IS NULL filter present via grep against src/trigger/ + src/lib/ingest/ + src/lib/sources/).

- **SC#3 — user ban revokes sessions AND Langfuse OPS-02 static check:**
  - Seed admin user + target user + 1 session row for target.
  - Call banUserCore. Assert: users.is_banned=true AND sessions.count(userId=target)=0.
  - Call getDailyCosts({ days: 1 }); expect an array (may be empty — just asserts the query succeeds against real pipeline_runs schema).
  - **BLOCKER-1 OPS-02 evidence (static assertions):**
    - Assert file `src/lib/llm/otel.ts` exists AND contains a case-insensitive match for `langfuse` (confirms Phase 3 Langfuse OTel wiring is present — e.g., `@langfuse/otel` import + `LangfuseSpanProcessor` usage).
    - Assert `.env.example` contains `LANGFUSE_PUBLIC_KEY` and `LANGFUSE_SECRET_KEY` declarations.
    - Assert `package.json` dependencies include `@langfuse/otel`.
    - If any of the above fails, OPS-02 cannot possibly satisfy — harness fails with a clear "OPS-02 static preconditions missing" message.
    - Live Langfuse dashboard confirmation happens in 06-UAT.md SC#3 (human-gated; requires project keys).

- **SC#4 — Sentry integration DEFERRED to UAT (requires live deploy + dashboard access).**
  - Harness checks that sentry.server.config.ts exists and contains 'beforeSend' and 'cookie' scrub — static file assertion.
  - Full live verification in 06-UAT.md Task 3 pattern.

- **SC#5 — dead-letter retry + sitemap:**
  - Seed 1 item with status='dead_letter'. Call retryItemCore. Assert status='pending', failureReason=null, retryCount incremented.
  - Call getPublishedItemUrls({limit:10}); assert result is an array; if any rows, assert none have status != 'published' (defense-in-depth).
  - HTTP probe: fetch http://localhost:3000/sitemap.xml (requires dev server running OR harness mocks); assert response contains '<urlset'. If no local server, skip with a warning (matches Phase 3 SC#5 deferral pattern).

Cleanup in finally{} — delete all sentinel rows.

Register in package.json:
```json
"verify:admin-ops": "tsx --env-file=.env.local scripts/verify-admin-ops.ts"
```

Create `.planning/phases/06-admin-operational-hardening/06-UAT.md` with the same structure as `03-UAT.md`:
- Preflight checklist (env vars set, admin user promoted, dev server running, Sentry DSN configured, Vercel deployment URL known)
- SC-by-SC manual verification steps
- SC#1: visit /admin as non-admin → redirect to /admin/access-denied; visit as admin → nav loads
- SC#2: create source, edit weight, toggle active, soft-delete; observe source disappears from admin list AND from the /api/health sources count
- SC#3: ban a user while they have an open session in another browser tab; that browser must become anonymous on next request; /admin/costs shows rows. **OPS-02 live check:** visit `https://cloud.langfuse.com/project/<LANGFUSE_PROJECT_ID>/traces` within 5 minutes of a live pipeline run — assert ≥1 trace exists from today with non-zero cost and cache_read_input_tokens metadata visible.
- SC#4: throw a deliberate error via /api/admin/sentry-test; verify Sentry dashboard shows event within 5 min with PII scrubbed; same for Trigger.dev task
- SC#5: manually set an item's status to 'dead_letter' via SQL; visit /admin/dead-letter; click retry; verify item returns to pending + processes; curl /sitemap.xml; verify valid XML without /admin URLs
- Sign-off block: verifier, date, branch, verdict (PASS/FAIL per SC)
- Troubleshooting table with root cause + fix for common failures
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit scripts/verify-admin-ops.ts 2>&1 | head -20 && test -f .planning/phases/06-admin-operational-hardening/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-admin-ops.ts exists
    - `grep -c "SC#" scripts/verify-admin-ops.ts` returns >= 5
    - `grep -q "verify:admin-ops" package.json` returns 0
    - .planning/phases/06-admin-operational-hardening/06-UAT.md exists
    - 06-UAT.md contains "SC#1" through "SC#5" sections
    - 06-UAT.md contains "Sign-off" section
    - **BLOCKER-1 OPS-02 evidence assertions embedded in the harness:**
      - `grep -riq "langfuse" src/lib/` returns 0 (Phase 3 Langfuse wiring present — Langfuse exporter / span processor)
      - `grep -q "LANGFUSE_PUBLIC_KEY" .env.example` returns 0
      - `grep -q "LANGFUSE_SECRET_KEY" .env.example` returns 0
      - `grep -q "@langfuse/otel" package.json` returns 0
      - `grep -q "cloud.langfuse.com" .planning/phases/06-admin-operational-hardening/06-UAT.md` returns 0 (SC#3 human check references live dashboard URL)
    - **WARNING-10 (SC#1 no dev-server):** `grep -q "requireAdmin" scripts/verify-admin-ops.ts` returns 0 AND `! grep -q "fetch.*:3000/admin" scripts/verify-admin-ops.ts` returns 0 (no HTTP probe for SC#1)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Verification harness + UAT ready for Task 3 live run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live Phase 6 UAT — all 5 SCs end-to-end</name>
  <what-built>All Phase 6 plans (06-00 through 06-07) plus runbooks + verification harness from Tasks 1-2.</what-built>
  <read_first>.planning/phases/06-admin-operational-hardening/06-UAT.md, scripts/verify-admin-ops.ts, docs/admin.md, docs/observability.md</read_first>
  <action>
Execute the full UAT protocol:

1. Run `pnpm verify:admin-ops` — assert exits 0 with all SC# checks passing (SC#4 Sentry portion skipped to UAT).
2. Walk through 06-UAT.md manually on a live Vercel deployment (or dev server if Vercel env not ready).
3. Complete the sign-off block in 06-UAT.md.
4. If any SC fails, file a gap closure plan (`--gaps` flag) rather than patching directly.
  </action>
  <how-to-verify>
    1. pnpm verify:admin-ops exits 0
    2. All 5 ROADMAP success criteria verified manually per 06-UAT.md steps
    3. Sentry dashboard shows deliberate errors from BOTH Next.js and Trigger.dev within 5 min with PII scrubbed
    4. User ban: banned user's second browser tab treated as anonymous on next request
    5. Dead-letter retry: item cycles back through LLM pipeline and reaches status='published' (may take a few minutes for the cron to pick it up)
    6. sitemap.xml valid XML; robots.txt disallows /admin; no Google Analytics beacon on page load
    7. 06-UAT.md sign-off block filled out
  </how-to-verify>
  <resume-signal>Type "approved" with per-SC PASS verdicts recorded in 06-UAT.md.</resume-signal>
  <acceptance_criteria>
    - pnpm verify:admin-ops exits 0
    - .planning/phases/06-admin-operational-hardening/06-UAT.md sign-off block filled in with PASS verdicts for all 5 SCs
    - Sentry dashboard screenshot or event-ID referenced in the UAT sign-off
    - All operational pages reachable: /admin, /admin/sources, /admin/users, /admin/costs, /admin/dead-letter
    - /sitemap.xml returns 200 with valid XML
  </acceptance_criteria>
  <done>Phase 6 operational hardening proven end-to-end on a live deployment; runbooks in place for future operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification harness → live dev DB | Seeds + cleans test rows; runs with DATABASE_URL |
| runbook readers → production ops | Knowledge transfer; no direct runtime exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-80 | Tampering | verify harness leaves orphan rows | mitigate | Cleanup in finally{} identical to verify-llm.ts pattern — mirrors 03-05 discipline. If the harness crashes, rerun is idempotent. |
| T-6-81 | Information Disclosure | runbooks in repo expose ops secrets | mitigate | Runbooks document WHICH env vars to set, never the values. Matches docs/auth-providers.md convention. |
| T-6-82 | Elevation of Privilege | runbook describes SQL role promotion | accept | By design — the runbook IS the one-off admin bootstrap flow per CONTEXT 05 §Specifics. Protected by DATABASE_URL access which is already admin-only. |
</threat_model>

<verification>
- pnpm verify:admin-ops exits 0
- docs/admin.md and docs/observability.md exist and are referenced in README (or equivalent index)
- 06-UAT.md sign-off captured
</verification>

<success_criteria>
OPS-02 documented (no code change — Phase 3 Langfuse wiring remains canonical). All 5 Phase 6 ROADMAP success criteria verified end-to-end via harness + UAT. Runbooks ship.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-operational-hardening/06-08-SUMMARY.md`
</output>
