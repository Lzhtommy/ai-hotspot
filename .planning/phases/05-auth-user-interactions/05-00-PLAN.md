---
phase: 05
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - playwright.config.ts
  - tests/setup.ts
  - tests/helpers/db.ts
  - tests/helpers/auth.ts
  - tests/helpers/seed-session.ts
  - tests/unit/schema-auth.test.ts
  - tests/unit/schema-users-extension.test.ts
  - tests/unit/auth-config.test.ts
  - tests/unit/session-payload.test.ts
  - tests/unit/provider-github.test.ts
  - tests/unit/provider-resend.test.ts
  - tests/unit/provider-google.test.ts
  - tests/unit/login-prompt-modal.test.tsx
  - tests/unit/login-prompt-modal-magic-link.test.tsx
  - tests/unit/user-chip.test.tsx
  - tests/unit/user-chip-signout.test.tsx
  - tests/unit/feed-card-actions.test.tsx
  - tests/unit/vote-honest-copy.test.tsx
  - tests/unit/vote-value-contract.test.ts
  - tests/unit/env-remote-patterns.test.ts
  - tests/integration/ban-enforcement.test.ts
  - tests/integration/server-action-favorite.test.ts
  - tests/integration/server-action-vote.test.ts
  - tests/integration/server-action-auth-guard.test.ts
  - tests/integration/favorites-page.test.tsx
  - tests/e2e/auth-github.spec.ts
  - tests/e2e/auth-magic-link.spec.ts
  - tests/e2e/anon-login-favorite.spec.ts
  - tests/e2e/ban-enforcement.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vitest and Playwright installed and runnable via pnpm scripts"
    - "All 22 Phase 5 test stub files exist and fail in a known red state (expected before implementation)"
    - "Test helpers exist for DB access, fake session construction, and seeded session cookies"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest jsdom environment + path aliases matching tsconfig"
    - path: "playwright.config.ts"
      provides: "Playwright config with webServer + baseURL http://localhost:3000"
    - path: "tests/setup.ts"
      provides: "Shared testing-library setup"
    - path: "tests/helpers/auth.ts"
      provides: "fakeSession() helper + authenticatedContext() wrapper"
    - path: "tests/helpers/seed-session.ts"
      provides: "Inserts row into sessions table; returns cookie for Playwright storageState"
    - path: "tests/unit/schema-auth.test.ts"
      provides: "Red-state assertion for accounts/sessions/verification_tokens schema"
  key_links:
    - from: "package.json"
      to: "vitest + @playwright/test scripts"
      via: "scripts.test + scripts.test:e2e"
      pattern: "vitest run"
---

<objective>
Install Vitest + Playwright infrastructure and create 22 red-state test stubs per 05-VALIDATION.md Wave 0. This establishes the feedback loop before any implementation begins — tests fail meaningfully (unresolved imports → implementation → green) so sampling during Waves 1–5 reflects real progress.

Purpose: Nyquist-compliant validation requires test files to exist before implementation so `pnpm test --changed` reflects code completion status.

Output: Configured test frameworks, shared helpers, and stub files at every path listed in 05-VALIDATION.md §Per-Task Verification Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-user-interactions/05-CONTEXT.md
@.planning/phases/05-auth-user-interactions/05-VALIDATION.md
@.planning/phases/05-auth-user-interactions/05-PATTERNS.md
@.planning/phases/05-auth-user-interactions/05-RESEARCH.md

<interfaces>
From src/lib/feed/get-feed.test.ts (co-located test pattern established Phase 4):
```typescript
import { describe, expect, it, vi, type Mock } from 'vitest';
// Uses vi.mock('@/lib/db/client') to inject mock db
```

From tests/e2e/all.spec.ts (existing Playwright pattern):
```typescript
import { test, expect } from '@playwright/test';
test('...', async ({ page }) => {
  await page.goto('/');
  await expect(page.getByRole('heading', { level: 1 })).toBeVisible();
});
```

Package manager: pnpm 10.32.1 (per package.json.packageManager).
Existing Phase 4 test paths: tests/e2e/ (Playwright) + co-located *.test.ts/*.test.tsx (Vitest).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies + configure Vitest + Playwright</name>
  <read_first>
    - package.json (current dependencies + scripts)
    - vitest.config.ts (may exist from Phase 2+; read before modifying)
    - playwright.config.ts (exists from Phase 4)
    - tests/e2e/all.spec.ts (canonical Playwright example)
    - src/lib/feed/get-feed.test.ts (canonical Vitest example)
    - tsconfig.json (for path alias reuse)
  </read_first>
  <action>
    Verify vitest, @vitest/ui, @testing-library/react, @testing-library/jest-dom, jsdom, @playwright/test are in package.json devDependencies. If any are missing, run `pnpm add -D <missing>`.

    Ensure `vitest.config.ts` at repo root has:
    - `test.environment: 'jsdom'`
    - `test.setupFiles: ['./tests/setup.ts']`
    - `resolve.alias` mapping `@/` → `./src/` (matches tsconfig paths)
    - `esbuild.jsx: 'automatic'` (per STATE.md Phase 4 note on ESM-only plugin avoidance)

    Ensure `playwright.config.ts` has:
    - `use.baseURL: 'http://localhost:3000'`
    - `webServer: { command: 'pnpm dev', reuseExistingServer: !process.env.CI, url: 'http://localhost:3000' }`
    - `retries: process.env.CI ? 2 : 0`

    Create `tests/setup.ts` with exactly:
    ```ts
    import '@testing-library/jest-dom/vitest';
    ```

    Verify package.json scripts include:
    - `"test": "vitest run"`
    - `"test:watch": "vitest"`
    - `"test:e2e": "playwright test"`
    - `"test:e2e:ci": "playwright test"` (identical in CI or with --reporter flag)
    - `"test:integration": "vitest run tests/integration"`

    Use pnpm — do NOT use npm or yarn. Package manager is pnpm@10.32.1.
  </action>
  <verify>
    <automated>pnpm vitest --version && pnpm playwright --version && pnpm test -- --run tests/setup.ts 2>&1 | grep -qv "Cannot find"</automated>
  </verify>
  <done>
    `pnpm vitest --version` prints a version; `pnpm playwright --version` prints a version; `vitest.config.ts`, `playwright.config.ts`, `tests/setup.ts` exist; path alias `@/` resolves in test files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test helpers (db, auth, seed-session)</name>
  <read_first>
    - src/lib/db/client.ts (real db singleton — helper wraps this)
    - src/lib/db/schema.ts (users, favorites, votes shapes)
    - drizzle/seed-sources.ts (if exists — existing seed pattern)
    - .planning/phases/05-auth-user-interactions/05-VALIDATION.md (helper requirements)
    - .planning/phases/05-auth-user-interactions/05-PATTERNS.md §14 (test helper analog)
  </read_first>
  <action>
    Create `tests/helpers/db.ts` exporting:
    ```ts
    import { drizzle } from 'drizzle-orm/neon-http';
    import { neon } from '@neondatabase/serverless';
    import * as schema from '@/lib/db/schema';
    export function makeTestDb() {
      const url = process.env.DATABASE_URL;
      if (!url) throw new Error('DATABASE_URL required for integration tests');
      return drizzle(neon(url), { schema });
    }
    ```
    For unit tests, export a `makeMockDb()` factory that returns a typed stub with `select`, `insert`, `update`, `delete` as `vi.fn()` returning chainable mocks. Mirror shape used in `src/lib/feed/get-feed.test.ts`.

    Create `tests/helpers/auth.ts` exporting:
    ```ts
    import type { Session } from 'next-auth';
    export function fakeSession(overrides: Partial<Session['user']> = {}): Session {
      return {
        user: {
          id: '<fake-uuid-for-fixtures>',
          email: 'test@example.com',
          name: 'Test User',
          image: null,
          role: 'user',
          ...overrides,
        },
        expires: new Date(Date.now() + 30 * 24 * 60 * 60 * 1000).toISOString(),
      };
    }
    ```

    Create `tests/helpers/seed-session.ts` — inserts a row into the real `sessions` table on a Neon branch; returns `{ sessionToken, userId, cookie }` where cookie is the Playwright storageState shape for `authjs.session-token`. Reference 05-PATTERNS.md §14 for the "no-analog / fresh implementation" note.
    NOTE: This helper depends on the `sessions` table existing (Plan 01). In Wave 0 it may import from schema even if the SQL table is not yet created — the stub test files that use it MUST skip when table is missing.
  </action>
  <verify>
    <automated>pnpm test -- --run tests/helpers 2>&1 | grep -qE "(helpers|No tests found)"</automated>
  </verify>
  <done>
    Three helper files exist with exports matching the action; `import { fakeSession } from '@/../tests/helpers/auth'` resolves in a test file; TypeScript compiles via `pnpm typecheck` or equivalent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 22 red-state test stub files per 05-VALIDATION.md</name>
  <read_first>
    - .planning/phases/05-auth-user-interactions/05-VALIDATION.md §Per-Task Verification Map (authoritative list of 22 stub paths)
    - tests/helpers/auth.ts (created Task 2)
    - tests/helpers/db.ts (created Task 2)
    - src/lib/feed/get-feed.test.ts lines 1-58 (Vitest manual-mock pattern)
    - tests/e2e/all.spec.ts lines 1-9 (Playwright pattern)
  </read_first>
  <action>
    Create each of the 22 test files listed in 05-VALIDATION.md with a minimal red-state test body. Each file MUST:
    1. Import `describe`, `it`, `expect` from `vitest` (or `test`, `expect` from `@playwright/test` for e2e files)
    2. Contain ONE `it('TODO: <task-id> <requirement-id>')` or `test(...)` that calls `expect(false).toBe(true)` with an explanatory message, OR `expect(() => require('<not-yet-created-module>')).not.toThrow()` for shape-imports that will fail until implementation lands
    3. Include the task-id, requirement-id, and threat-ref from 05-VALIDATION.md as a comment at the top:
    ```ts
    // Task 5-NN-MM | Plan NN | REQ-XX | Threat T-5-YY
    // Nyquist stub — red until implementation lands.
    ```

    Exact 22 file paths (reproduce verbatim; do NOT omit any):
    Unit (13):
    - tests/unit/schema-auth.test.ts
    - tests/unit/schema-users-extension.test.ts
    - tests/unit/auth-config.test.ts
    - tests/unit/session-payload.test.ts
    - tests/unit/provider-github.test.ts
    - tests/unit/provider-resend.test.ts
    - tests/unit/provider-google.test.ts
    - tests/unit/login-prompt-modal.test.tsx
    - tests/unit/login-prompt-modal-magic-link.test.tsx
    - tests/unit/user-chip.test.tsx
    - tests/unit/user-chip-signout.test.tsx
    - tests/unit/feed-card-actions.test.tsx
    - tests/unit/vote-honest-copy.test.tsx
    - tests/unit/vote-value-contract.test.ts
    - tests/unit/env-remote-patterns.test.ts

    Integration (5):
    - tests/integration/ban-enforcement.test.ts
    - tests/integration/server-action-favorite.test.ts
    - tests/integration/server-action-vote.test.ts
    - tests/integration/server-action-auth-guard.test.ts
    - tests/integration/favorites-page.test.tsx

    E2E (4):
    - tests/e2e/auth-github.spec.ts
    - tests/e2e/auth-magic-link.spec.ts
    - tests/e2e/anon-login-favorite.spec.ts
    - tests/e2e/ban-enforcement.spec.ts

    Total: 22 stubs. Each test SHOULD fail when run (that's the point — sampling shows meaningful progress when they flip green).
  </action>
  <verify>
    <automated>ls tests/unit/*.test.* tests/integration/*.test.* tests/e2e/*.spec.* | wc -l | grep -q "^\s*22\s*$" && pnpm vitest run tests/unit tests/integration 2>&1 | grep -qE "(failed|FAIL)"</automated>
  </verify>
  <done>
    22 files exist at the exact paths listed; each file contains at least one failing test or not-yet-resolvable import; `pnpm vitest run` reports them all as red without crashing the runner.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → repo | Test files run `db` queries against a real Neon branch in integration mode — must not mutate production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-W0-01 | Tampering | tests/helpers/db.ts | mitigate | Helper throws if DATABASE_URL points at production (hostname matches `ep-*-*-prod` or config contains 'prod') — fail-closed for integration tests |
| T-5-W0-02 | Information disclosure | tests/helpers/seed-session.ts | accept | Test session tokens are ephemeral + branch-scoped; no PII leaked |
</threat_model>

<verification>
- `pnpm vitest --version` + `pnpm playwright --version` both print versions
- 22 test stub files exist
- `pnpm vitest run` completes (may report failures — that's expected)
- All 3 helper files present and TypeScript-valid
</verification>

<success_criteria>
- Wave 0 infrastructure complete: all test frameworks configured, stubs created, helpers available
- `pnpm test` does not crash (red tests are acceptable and expected)
- Downstream plans (01-10) can append real assertions to stubs without path/import churn
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-user-interactions/05-00-SUMMARY.md`
</output>
